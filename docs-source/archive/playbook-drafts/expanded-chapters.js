// Expanded chapters 28-35 + Glossary for Arble Playbook
// Replace the corresponding entries in playbook-data.js

"28":{
part:"Production",
title:"Inference",
prev:{num:"27",title:"Hybrid Execution"},
next:{num:"29",title:"Routing"},
content:`<h2>The inference pipeline</h2>
<p>Inference in Arble is not a single operation — it is a five-stage pipeline that transforms a user message into an agent action. Each stage is managed by a separate runtime component, and the total latency from message to response is the sum of all five. Understanding these stages is essential for debugging performance, optimising costs, and diagnosing failures, because the model itself is only one part of the equation.</p>
<ol>
<li><strong>Preparation.</strong> The router selects a target model based on the classified request. The session manager assembles the context — system prompt, conversation history, tool schemas, and any memory results retrieved for the current turn. The assembled context is tokenised and measured against the model's window limit. If the context exceeds the limit, the compactor prunes low-signal turns or summarises blocks of conversation before the request is sent. Preparation also checks the response cache: if an identical input has been processed before, the cached result is returned and the remaining stages are skipped entirely.</li>
<li><strong>Transmission.</strong> The prepared context is delivered to the model endpoint. For cloud models, this is an HTTPS POST request to the provider's API, carrying authentication headers derived from the API key stored in SecureStore. For local models, the request is sent over a localhost connection to the running inference backend — Ollama, llama.cpp, or Core ML. Transmission time is dominated by network round-trip for cloud calls (typically 50–300 ms) and by context serialisation for local calls (under 10 ms).</li>
<li><strong>Generation.</strong> The model produces output tokens autoregressively — one token at a time, each conditioned on all previous tokens. In streaming mode, tokens are forwarded to the UI as they arrive, reducing perceived latency from the full generation time to the time-to-first-token. In non-streaming mode, the full response is buffered and returned as a single payload. Generation time depends on model size, quantisation level, hardware acceleration, and output length. A 7B parameter model on a Neural Engine generates roughly 30–50 tokens per second; a 70B model on a desktop GPU generates 15–25 tokens per second.</li>
<li><strong>Processing.</strong> The model's response is parsed and validated. If the model produced a tool call — a structured JSON object specifying a tool name and parameters — it is extracted and validated against the tool registry's schema. Malformed or hallucinated tool calls are rejected and logged. Text content is sanitised and inserted into the session as a new turn. If the model produced multiple tool calls in a single response, they are dispatched in parallel by the QueryEngine.</li>
<li><strong>Post-processing.</strong> After the response is incorporated into the session, the compactor evaluates whether the new turn requires pruning of older turns. The memory manager scans the response for extractable facts — names, dates, preferences, project references — and writes them to the semantic memory store if they meet the importance threshold. Usage metrics are updated: token counts per model, cumulative cost estimates, latency breakdowns. The agent loop then decides whether the goal is satisfied or another iteration is needed.</li>
</ol>
<p>The five stages form a pipeline within every turn of the agent loop. Optimisation efforts focus on transmission and generation, which together account for 95% or more of total wall-clock time. Preparation and post-processing are kept under 50 ms combined through efficient data structures and lazy evaluation.</p>
<h2>Streaming architecture</h2>
<p>Streaming is the practice of delivering model output tokens incrementally rather than waiting for the complete response. For chat interactions, this transforms the user experience: instead of a blank screen for 10 seconds followed by a full response, the user sees text begin to appear after 1–2 seconds and continue flowing naturally. The perceived latency of a streamed response is the time to first token, not the time to last token.</p>
<p>Arble's streaming implementation is provider-agnostic. Each model adapter normalises the provider's native streaming format into a standard internal representation. For Anthropic, streaming uses SSE events with content blocks. For OpenAI, streaming uses SSE events with delta chunks. For OpenAI-compatible endpoints, the format varies but is always convertible to a stream of token deltas. For local models, llama.cpp streams tokens over HTTP SSE, and Ollama streams via its native API. The streaming adapter in each backend converts these into a uniform event stream, and the UI renders all streams identically regardless of the underlying provider.</p>
<p>Tool calls are not streamed to the UI. When the model generates a tool call, it produces a complete JSON object — tool name and parameter map — in a single logical step. The system buffers this internally until the JSON is complete and valid, then dispatches it. The user sees the tool call appear at once with an execution status indicator, rather than watching JSON characters appear incrementally. This decision is deliberate: the intermediate tokens of a JSON object carry no semantic meaning for the user, and displaying them would add visual noise without informational value.</p>
<p>Streaming is supported for every cloud provider that offers it and for all local backends. The UI degrades gracefully for providers that do not support streaming: the full response is buffered and displayed in a single update, with a loading indicator shown during generation.</p>
<h2>Three-level caching</h2>
<p>Repeated inference calls are the largest source of unnecessary cost and latency in any AI system. Arble implements three levels of caching that operate at different granularities, each targeting a specific pattern of repetition:</p>
<ul>
<li><strong>Prompt cache.</strong> Cloud providers that support prompt caching — Anthropic and OpenAI are the primary examples — automatically detect repeated system prompt prefixes and serve the cached key-value representations rather than reprocessing them through the full transformer stack. In a multi-turn session, the system prompt and early conversation history are identical across consecutive requests, making prompt caching highly effective. Cache hit rates of 60–80% are typical for long sessions. Arble's adapters send cache-control headers to maximise hit rates, and the router logs prompt cache statistics per provider so the user can verify savings.</li>
<li><strong>Response cache.</strong> If the exact same input — system prompt, conversation history, tool schemas, user message — has been processed before, the result can be served from cache without calling the model at all. The response cache is keyed on a SHA-256 hash of the full prepared input. Cache entries are invalidated automatically when any component of the input changes: a new turn, a modified system prompt, a different tool selection, a different model endpoint. The response cache is most effective for repeated operations with identical parameters, such as recurring memory searches, status checks, or template-based requests. Cache entries are stored in MMKV with a configurable size limit (default 100 MB) and are automatically pruned using an LRU eviction policy.</li>
<li><strong>Embedding cache.</strong> Memory search requires generating vector embeddings for query text. If the same query appears multiple times — common for recurring operations like "find my recent documents" or "what did we discuss about Project X" — the embedding is served from cache rather than recomputed. The embedding cache is separate from the response cache because embedding models are typically smaller and faster than generation models, but caching still avoids redundant computation across sessions. The embedding cache persists across app restarts and is shared by all agents.</li>
</ul>
<p>All three caches are local to the device and never shared between users or transmitted to any server. Caching is transparent to the agent and the user. The activity log notes when a response was served from cache, and the metrics dashboard shows cumulative cache savings for the current billing period.</p>
<h2>Latency management and optimisation</h2>
<p>Inference latency is the single largest factor in perceived agent responsiveness. A model that produces perfect answers but takes 30 seconds per response makes the agent feel broken. Arble manages latency through a combination of architectural choices, runtime decisions, and user-configurable policies:</p>
<ul>
<li><strong>Model selection.</strong> The router prefers faster models for interactive conversations. A simple factual query — "what is the capital of France?" — uses a 3B parameter local model or a fast cloud model like GPT-4o-mini, with response times under 500 ms. Complex reasoning tasks wait for a flagship model. The latency penalty of a slow model is only paid when the task genuinely requires it. The router maintains a moving average of per-model latency and factors it into every selection decision.</li>
<li><strong>Parallel dispatch.</strong> When the agent proposes multiple independent tool calls in a single turn, the QueryEngine dispatches them concurrently rather than sequentially. The total wall-clock time is the maximum of the individual call times, not the sum. For model calls, this means the router can send requests to multiple candidate models in parallel and use the first successful response, a pattern known as "speculative routing."</li>
<li><strong>Timeout management.</strong> Every model endpoint has a configurable timeout — 30 seconds for cloud models, 60 seconds for local models by default. If an endpoint exceeds the timeout, the router falls back to the next model in the chain. The agent does not block waiting for a slow response; it moves on and attempts an alternative. Timeout violations are logged and contribute to the endpoint's degradation score.</li>
<li><strong>Background pre-warming.</strong> When the runtime detects that the user is likely to start a session — the app is foregrounded, the keyboard appears, a notification is tapped — it pre-warms the preferred local model and pre-establishes TLS connections to cloud providers. Pre-warming loads the model weights into memory or allocates GPU resources before the first inference call, shaving 500–2000 ms from the initial response time. Pre-warmed models are held for a configurable idle timeout (default 5 minutes) before unloading.</li>
</ul>
<h2>Error handling and reliability</h2>
<p>Inference errors are inevitable in a system that spans local devices and cloud APIs. Cloud providers return HTTP 5xx errors, rate-limit responses, and connection timeouts. Local model processes crash due to memory pressure or GPU driver issues. The runtime handles each failure mode with a specific recovery strategy:</p>
<ul>
<li><strong>Transient errors.</strong> HTTP 429 (rate-limited), 503 (service unavailable), and 504 (gateway timeout) trigger an automatic retry with exponential backoff: 1 second, 2 seconds, 4 seconds. After three consecutive failures, the router marks the endpoint as degraded and falls back to the next priority endpoint. Transient errors on different requests do not accumulate — each request starts with a fresh retry budget.</li>
<li><strong>Authentication errors.</strong> HTTP 401 (unauthorised) indicates the API key is invalid, expired, or revoked. The router immediately removes the endpoint from the active pool and notifies the user via a system notification. No retry is attempted, because retrying with the same credentials will produce the same result. The user can update the API key in settings and re-test the endpoint.</li>
<li><strong>Malformed responses.</strong> If the model returns invalid JSON for a tool call, or returns a tool name not present in the registry, the runtime logs the error with the full response payload and requests a corrected response from the model. The request includes the error message so the model can learn from the mistake. If the model produces malformed output three times consecutively within a single turn, the session is paused and the user is notified with diagnostic information.</li>
<li><strong>Local model crashes.</strong> If a local model process exits unexpectedly — out of memory, segfault, driver crash — the runtime detects the process exit via the backend's health monitor, restarts the model backend, and falls back to a cloud model for the current request. The crash is logged with diagnostic information including memory pressure at the time of the crash and the model configuration.</li>
</ul>
<p>The error handling system is designed to keep the agent working through failures end-to-end. In most cases, the user sees no error — the system absorbs it, retries, falls back, and continues. Only when all endpoints in the fallback chain have been exhausted does the agent pause and report the failure to the user.</p>`
},

"29":{
part:"Production",
title:"Routing",
prev:{num:"28",title:"Inference"},
next:{num:"30",title:"Security"},
content:`<h2>The model routing problem</h2>
<p>An AI operating system that supports multiple models must decide which one to use for every request. The wrong choice wastes money — routing a trivial lookup to a flagship model burns expensive context windows on a task a 3B parameter model could handle. It hurts latency — sending a quick question to a slow model makes the agent feel unresponsive. And it produces poor results — using a weak model for a complex reasoning task generates low-quality output that the user must then correct.</p>
<p>Model routing is an optimisation problem with multiple objectives: quality, cost, latency, privacy, and availability. These objectives often conflict. The cheapest model may be too slow. The fastest model may lack the capability for complex tasks. The most private model (local) may not support the required modality. Arble's model router resolves these conflicts through a three-layer architecture that classifies each request, evaluates available endpoints against the request's requirements, and executes the selected call with fallback protection.</p>
<h2>Three-layer router architecture</h2>
<p>The router is organised as three discrete layers that operate sequentially. Each layer has a specific responsibility and a well-defined interface with the next:</p>
<ul>
<li><strong>Classifier.</strong> The classifier analyses the incoming request and extracts a feature vector that guides the selection decision. It identifies the task type: code generation, factual retrieval, creative composition, reasoning and analysis, tool selection, or classification and extraction. It estimates complexity on a three-point scale: simple (one-shot, no tools needed), medium (multi-step with tools), complex (multi-step with conditional logic and external data). It checks modality requirements — text-only or multimodal (vision, audio). It assesses data sensitivity by scanning for patterns that suggest personal or confidential information: email addresses, financial data, health-related terms. The classifier is implemented as a set of pattern matchers and a lightweight classification model that runs locally. Classification completes in under 5 ms.</li>
<li><strong>Selector.</strong> The selector matches the classified request against the available endpoints. Each endpoint in the registry has a capability profile: which task types it handles well, its supported modalities, its cost per million tokens, its typical latency distribution (p50 and p95), its privacy tier (local, trusted cloud, standard cloud), and its current health status (healthy, degraded, offline). The selector computes a score for each eligible endpoint by weighting the request's requirements against the endpoint's profile. The scoring function is configurable and supports the five routing strategies described below. The highest-scoring endpoint is selected for execution. Selection completes in under 10 ms for registries with up to 50 endpoints.</li>
<li><strong>Executor.</strong> The executor sends the request to the selected endpoint, monitors the response, and handles failures. It manages the HTTP connection lifecycle for cloud endpoints — connection pooling, TLS handshake reuse, request timeouts. For local endpoints, it manages the IPC channel to the inference backend. The executor tracks per-endpoint metrics: response time, error rate, tokens generated, cache hit rate. If the endpoint fails, the executor consults the fallback chain and retries with the next endpoint. The executor is the only layer that makes network calls; the classifier and selector are purely local operations.</li>
</ul>
<p>The three layers are decoupled and independently testable. A unit test can verify that a given request classification produces the expected endpoint selection without executing any model calls. Integration tests exercise the full pipeline against mock endpoints.</p>
<h2>Five routing strategies</h2>
<p>The selector's scoring function can be configured to optimise for different objectives. Arble supports five strategies, each designed for a specific usage pattern:</p>
<ul>
<li><strong>Cost-optimised.</strong> Prefers the cheapest endpoint that meets the minimum quality threshold for the classified task type. For a simple factual query, this means a 3B local model or a budget cloud endpoint like GPT-4o-mini. For a complex reasoning task, the threshold rises and a flagship model is selected. The cost-optimised strategy uses a lookup table of minimum quality requirements per task type, derived from empirical testing during development. This is the default strategy for most sessions.</li>
<li><strong>Quality-optimised.</strong> Always selects the highest-capability endpoint available for the task, regardless of cost. Used for high-stakes tasks where quality is paramount — legal analysis, medical information processing, critical code review. The quality-optimised strategy ignores the cost dimension entirely and selects the endpoint with the highest capability score for the task type.</li>
<li><strong>Latency-optimised.</strong> Prefers the fastest endpoint that can handle the task, even if it costs more per token. Used for interactive conversations where response time is the primary UX factor. The latency-optimised strategy uses the p50 latency metric from each endpoint's capability profile, selecting the endpoint with the lowest expected response time. For very simple tasks, this often selects a local model because the network round-trip to a cloud endpoint is avoided.</li>
<li><strong>Privacy-optimised.</strong> Prefers local endpoints for all tasks. Cloud endpoints are used only when no local model can handle the task — for example, when the task requires a modality (vision, audio) that the local model does not support, or when the complexity exceeds the local model's capability. The privacy-optimised strategy applies a large negative weight to cloud endpoints in the scoring function, ensuring they are selected only as a last resort.</li>
<li><strong>Manual.</strong> The user specifies the preferred model for specific task types or for the entire session. Manual overrides take precedence over all other strategies. A user can pin "always use Claude 4 Opus for code review" while leaving all other tasks to the default strategy. Manual rules are stored in the permission and configuration system and are synced across devices.</li>
</ul>
<p>Strategies can be changed at any time during a session. The user can start a conversation with the latency-optimised strategy and switch to quality-optimised for a specific complex request. The router re-evaluates the strategy setting before every selection decision.</p>
<h2>Fallback chains</h2>
<p>No model endpoint is perfectly reliable. Cloud APIs return errors, exhaust rate limits, and experience outages. Local models can crash or be removed from the device. The fallback chain is the router's mechanism for maintaining service through endpoint failures.</p>
<p>Each request is associated with a fallback chain: an ordered list of endpoints to try, in priority order. The chain is generated dynamically based on the request classification and the current strategy. For a quality-optimised request, the chain might be: Claude 4 Opus (primary), GPT-4.5 (first fallback), Claude 3.5 Sonnet (second fallback), local 7B model (last resort). For a latency-optimised request, the chain might be: local 7B model (primary), GPT-4o-mini (first fallback), Claude 3.5 Haiku (second fallback).</p>
<p>The executor walks the fallback chain sequentially. If the primary endpoint returns a successful response, the chain is complete. If the primary endpoint fails — returns an error, times out, produces a malformed response — the executor moves to the next endpoint in the chain. The failure is logged with the error details and the endpoint identity. If all endpoints in the chain fail, the executor returns a comprehensive error to the agent, which pauses the session and notifies the user.</p>
<p>Fallback decisions are informed by endpoint health data. The executor does not retry an endpoint that is currently marked as degraded by the health monitoring system — it skips directly to the next endpoint in the chain. This prevents wasted time on endpoints that are known to be failing.</p>
<h2>Endpoint health monitoring</h2>
<p>The router maintains a real-time health status for every endpoint in the registry. Health is assessed through four signals:</p>
<ul>
<li><strong>Health checks.</strong> Each endpoint is pinged periodically with a lightweight request — typically a short completion prompt that costs minimal tokens. A configurable interval (default 60 seconds for cloud, 120 seconds for local) determines how frequently the check runs. Failed health checks increment a failure counter; successful checks reset it. When the counter exceeds a threshold (default 3), the endpoint is marked as degraded.</li>
<li><strong>Error tracking.</strong> Every error response from an endpoint is recorded: HTTP status code, error message, timestamp. The router tracks the error rate per endpoint over a rolling 5-minute window. If the error rate exceeds a configurable threshold (default 10%), the endpoint is marked as degraded. The error rate is decayed over time so a temporary spike does not permanently penalise an endpoint.</li>
<li><strong>Latency monitoring.</strong> The router maintains a moving average of response times per endpoint, computed over the last 50 requests. If the current latency exceeds the endpoint's baseline p95 by more than 2x, the endpoint is marked as degraded. Latency degradation often precedes complete failure and provides early warning of provider-side issues.</li>
<li><strong>Rate-limit tracking.</strong> HTTP 429 responses are tracked separately from other errors. If an endpoint returns three or more rate-limit responses within a 5-minute window, the router marks it as degraded with a rate-limit-specific status. Rate-limited endpoints are skipped in the fallback chain until the cooldown period expires (default 60 seconds).</li>
</ul>
<p>Health status is stored in the runtime state and is recalculated after every request and every health check. Degraded endpoints are deprioritised but not removed from the registry. If all endpoints are degraded, the router falls back to the least-degraded option and notifies the user that model availability is reduced. When an endpoint returns to healthy status — sustained successful responses over a 5-minute observation period — it is restored to the active pool.</p>` 
},

"30":{
part:"Production",
title:"Security",
prev:{num:"29",title:"Routing"},
next:{num:"31",title:"Observability"},
content:`<h2>Security model overview</h2>
<p>Arble's security model is built on the principle of least privilege applied to AI agents. The agent should have the minimum necessary access to accomplish the user's goals, and every access should be justifiable. This is the same principle that governs user accounts in traditional operating systems, applied to the novel context of an autonomous AI system that makes decisions and takes actions on the user's behalf.</p>
<p>The model operates at four layers, each addressing a different part of the attack surface. The layers are independent — a compromise at one layer does not automatically compromise the others. This defence-in-depth approach ensures that even if an attacker gains access to the device's network, they still cannot read stored data without also breaching the encryption layer, and they cannot execute tool calls without also bypassing the permission gate.</p>
<h2>Layer one: device security</h2>
<p>The runtime uses the device's built-in security hardware and operating system protections as its foundation. On iOS, Arble uses the Secure Enclave for cryptographic key generation and storage. The Secure Enclave is a dedicated hardware security coprocessor that isolates key material from the main processor, so even if the application process is compromised, the keys cannot be extracted. On Android, equivalent protections are provided by the Titan M security module or the Android Keystore system, with hardware-backed key storage on supported devices.</p>
<p>Sensitive data is encrypted at rest using the device's hardware-backed encryption keys. The runtime stores session data, configuration, and cached responses in encrypted storage. The encryption keys are derived from the device's unique identifier and the user's authentication biometrics, so the data cannot be decrypted on any other device. When the app is backgrounded, sensitive data is purged from memory and reloaded only when the user re-authenticates.</p>
<p>Biometric authentication — Face ID, Touch ID, or fingerprint — is required for high-sensitivity operations: viewing stored API keys, exporting the memory store, clearing the audit log, and changing security settings. The authentication request is made through the operating system's native biometric APIs, which prevent the app from intercepting or storing biometric data.</p>
<h2>Layer two: network security</h2>
<p>All external network communication from Arble is encrypted using TLS 1.3 with strong cipher suites. Cloud model API calls are made over HTTPS to the provider's endpoints, with certificate validation enforced. The runtime uses certificate pinning for critical endpoints to prevent man-in-the-middle attacks from compromised certificate authorities.</p>
<p>Local network pairing between Arble mobile and the desktop agent uses a multi-factor handshake designed to prevent unauthorised access even on shared networks. The pairing process begins with mDNS discovery: the desktop agent broadcasts its presence and a pairing token on the local network. The mobile app discovers the broadcast and initiates a connection. Both devices display a verification code derived from the pairing token and the device's hardware key. The user confirms the pairing by verifying that both devices show the same code — the same trust-on-first-use pattern used by AirDrop and Bluetooth pairing. After verification, the devices exchange public keys signed by their respective Secure Enclaves. All subsequent communication is encrypted end-to-end using these keys.</p>
<p>The desktop agent generates a self-signed TLS certificate on first launch. The certificate fingerprint is shared with the mobile app during the pairing handshake. On every subsequent connection, the mobile app verifies that the desktop agent's certificate matches the stored fingerprint. This certificate pinning ensures that even if an attacker intercepts the mDNS broadcast or the WebSocket connection, they cannot impersonate the desktop agent without possessing its private key, which never leaves the desktop's Secure Enclave or Keychain.</p>
<p>No cloud relay is involved in local pairing. The connection is direct between devices over the local network, using WebSocket with TLS. If the devices are on different networks, the user can configure a relay, but the relay sees only encrypted blobs and cannot decrypt the communication.</p>
<h2>Layer three: permission security</h2>
<p>The permission gate is the runtime component that evaluates every tool call before execution. It is implemented in trusted code that the agent cannot modify, override, or bypass. The agent proposes a tool call — a tool name and a parameter map — and the permission gate evaluates it against the user's configured rules. The gate returns one of three verdicts: allow (execute immediately), ask (pause and request user approval), or deny (reject without execution).</p>
<p>Tool calls are categorised by their side effects into five groups, each with a default permission level: Read operations (search, file read, weather check) default to auto-approve. Write operations (file create, email send, calendar add) default to ask. Send operations (message post, notification send, payment) default to ask and are always subject to user approval in the current implementation. Execute operations (shell command, code run, deploy) default to ask and are blocked unless explicitly enabled. Configure operations (setting change, permission update, toolset install) default to blocked and require explicit user enable in the security settings.</p>
<p>The permission gate is context-aware. A tool call from a background agent that the user has not interacted with recently is more likely to require approval than the same tool call from an active conversation. The gate considers the agent's trust level, the session's age, the sensitivity of the parameters, and whether the tool call is part of a known skill. This contextual evaluation reduces unnecessary interruptions without compromising security.</p>
<h2>Layer four: data security</h2>
<p>User data is stored locally on the device and encrypted at rest. The memory store — the vector database containing facts, preferences, and knowledge extracted from conversations — is encrypted using a device-derived key. Individual memory entries are not encrypted separately; the entire database file is encrypted at the storage layer using SQLCipher, a SQLite extension that provides transparent 256-bit AES encryption.</p>
<p>API keys for cloud model providers are the most sensitive credentials in the system. They are stored exclusively in SecureStore — iOS Keychain on Apple devices, EncryptedSharedPreferences on Android. Keys are never written to disk unencrypted, never logged, and never included in session history or memory entries. They are loaded into memory only at the moment of a model API call and are immediately zeroed from the memory buffer after the call completes. The key management UI allows users to add, remove, and test keys, but the key values are displayed as masked strings and can only be revealed through biometric authentication.</p>
<p>When memory is synchronised between devices, the sync data is encrypted end-to-end before transmission. The encryption keys are derived from the user's device key and the pairing relationship, so the sync relay server — if one is used — stores only encrypted blobs and cannot read the contents. Sync is not enabled by default; the user must explicitly enable it and configure which data categories to sync.</p>
<h2>Immutable audit log</h2>
<p>Security is not just about preventing unauthorised actions — it is also about making authorised actions visible and reviewable. Arble's audit log records every security-relevant event: every tool call with its parameters and verdict, every permission decision with the agent's reasoning, every model request with the model identity and token count, every network connection with the endpoint address and protocol, and every configuration change with the old and new values.</p>
<p>The audit log is stored locally and is immutable by design. Entries are append-only and are written before the corresponding action is executed. The agent cannot modify or delete log entries. The log is stored in a separate SQLite database with its own encryption key, isolated from the session and memory stores. If the audit database becomes corrupted, the runtime stops processing tool calls until the issue is resolved — audit integrity is treated as a hard dependency for agent operation.</p>
<p>The user can review the audit log in the app, filtered by event type, date range, or agent. The log can be exported as a JSON file for external review. Export requires biometric authentication. The audit log is the foundation of Arble's transparency model: the agent does not operate in a black box, and everything it does can be verified. Trust is not assumed — it is earned through verifiable behaviour.</p>`
},

"31":{
part:"Production",
title:"Observability",
prev:{num:"30",title:"Security"},
next:{num:"32",title:"Scaling"},
content:`<h2>Observability for autonomous systems</h2>
<p>Observability is the ability to understand what a system is doing, why it is doing it, and what happened in the past. For a traditional web service, observability means logs, metrics, and traces — tools that help engineers debug production issues. For an AI operating system, observability serves a different purpose: it helps the <em>user</em> understand and trust a system whose behaviour is generated, not programmed. You cannot read the source code to understand why an agent made a particular decision. You need observability data that captures the agent's context, reasoning, and actions.</p>
<p>Arble's observability system covers four domains: session history (what happened in each conversation), activity feed (what the agent is doing right now), performance metrics (how efficiently the system is running), and system health (whether the runtime itself is functioning correctly). All observability data is stored on-device. No usage data, metrics, or logs are transmitted to Arble or any third party. The user's operational data belongs to the user.</p>
<h2>Session history with full-text search</h2>
<p>Every session — every conversation with an agent — is persisted with its complete turn history. Each turn records the user message, the model response (including intermediate reasoning), any tool calls with their parameters and results, the permission decisions for each tool call, and metadata including token counts, latency per stage, and the model that handled the request. The session history is not a black box — every detail of what the agent did and why is captured.</p>
<p>The session history is searchable using SQLite FTS5, the full-text search engine built into SQLite. FTS5 provides fast, indexed search over the text content of all turns across all sessions. The search indexes the full text of user messages, model responses, tool call parameters, and tool results. It supports prefix queries, phrase queries, and Boolean operators. A user can search for "budget meeting March" and find every turn across every session where those terms appear. Search queries can be filtered by session date range, by agent identity, by tool used, or by permission decision type.</p>
<p>The search index is built incrementally. When a new turn is added to a session, the text content is indexed in the background within milliseconds. The index is stored separately from the session data to avoid I/O contention during active sessions. Full index rebuilds are triggered only when the FTS5 schema changes between Arble versions.</p>
<p>Session history is retained indefinitely by default, configurable per agent. The user can archive old sessions — the full turn history is compressed and stored in a separate archive database, but remains searchable. Individual sessions can be deleted permanently, which removes the turn history from both the active and archive databases. Memory entries extracted from deleted sessions are preserved unless the user explicitly deletes them from the memory store.</p>
<h2>Real-time activity feed</h2>
<p>The activity feed is a live-updating stream of the agent's current actions. It is displayed in the app's main screen and provides a window into what the agent is doing at every moment. The feed shows the agent's current phase (planning, executing, verifying), the current tool call with its status (pending, running, completed, failed), the model currently processing a request, any permission requests awaiting user action, and completion notifications for background tasks.</p>
<p>Each activity entry is a structured event with a timestamp, an agent identity, an action description, a status indicator, and a duration. Completed entries show the elapsed time; running entries show a live elapsed timer. Failed entries show the error message and the recovery action taken (retry, fallback, skip, or escalation). The feed provides a complete audit trail for the current session, refreshed in real-time.</p>
<p>The activity feed is stored locally with a configurable retention period, defaulting to 30 days. Entries older than the retention period are automatically archived — compressed into a summary format that preserves the event type and duration but omits the detailed parameters. Archived entries can be restored to full detail if needed by re-reading the corresponding session history. The 30-day default is chosen based on typical usage patterns: users rarely need to review activity details beyond a month, but the option to extend retention is available in settings.</p>
<h2>On-device performance metrics</h2>
<p>Arble tracks performance metrics to help users understand and optimise their usage. All metrics are computed on-device and stored locally. No metric data is transmitted to any server. The metrics dashboard shows:</p>
<ul>
<li><strong>Token accounting.</strong> Total tokens consumed per session, per model endpoint, and cumulatively across all sessions. The dashboard breaks down token usage into input tokens (context) and output tokens (generation). Cloud provider costs are estimated from token counts using the provider's published pricing, displayed in the user's configured currency.</li>
<li><strong>Latency breakdown.</strong> The total wall-clock time for each turn, broken down by stage: classification and selection (router), preparation (context assembly and tokenisation), transmission (network round-trip or IPC), generation (model inference), processing (response parsing), and post-processing (compaction and memory extraction). The breakdown helps users and developers identify where time is being spent in each interaction.</li>
<li><strong>Error tracking.</strong> Every error is recorded with its type, source, timestamp, and the context in which it occurred. Errors are categorised as transient (retried and resolved), permanent (required fallback), or critical (required user intervention). The error dashboard shows error rates per endpoint, per tool, and per session type.</li>
<li><strong>Budget tracking.</strong> If the user has configured a monthly spending budget for cloud model usage, the system tracks cumulative spend and displays progress toward the budget. Notifications are sent at configurable thresholds: 50%, 80%, 90%, and 100% of the budget. When the budget is exhausted, cloud model calls are blocked unless the user explicitly overrides the limit.</li>
</ul>
<p>The metrics data is retained for the same 30-day period as the activity feed. Historical metrics are summarised into daily and weekly aggregates after 30 days, preserving trends and totals while reducing storage overhead. The user can export the full metrics dataset as a CSV file for external analysis.</p>
<h2>Debugging agent behaviour through observability</h2>
<p>The most important function of observability in an AI system is enabling the user to understand why the agent acted as it did. When the agent makes an unexpected decision — calling a tool the user did not expect, refusing a request, producing an incorrect result — the observability system provides the data needed to diagnose the behaviour.</p>
<p>The session inspector shows the exact context that was sent to the model for each turn: the system prompt, the conversation history, the tool schemas, and any memory results. The user can see what the model "saw" when it made its decision. The model's raw response is displayed alongside the parsed tool calls and text output, so the user can verify that the system correctly interpreted the model's output.</p>
<p>Permission decisions include the agent's stated reasoning — the text the model generated that led it to propose a particular tool call. This reasoning is extracted from the model's response and displayed alongside the permission request. The user can see not just what tool the agent wanted to call, but why it believed the call was necessary. Over time, this transparency builds trust: the user can verify that the agent's reasoning is sound, and the agent's track record of sound reasoning earns it greater autonomy.</p>`
},

"32":{
part:"Production",
title:"Scaling",
prev:{num:"31",title:"Observability"},
next:{num:"33",title:"Ambient Computing"},
content:`<h2>Three axes of scaling</h2>
<p>Scaling an AI operating system is fundamentally different from scaling a web service. The constraints are not about handling more concurrent users — they are about handling more concurrent agents, more tools, more memory entries, and more complex sessions on a single device with finite memory, CPU, and battery. Arble scales across three independent axes, each with its own optimisation strategy:</p>
<ul>
<li><strong>Agents.</strong> The number of agents running simultaneously, each with its own session context, tool registry, and model state. More agents means more parallelism — background research while the interactive agent handles a conversation — but also more resource contention.</li>
<li><strong>Tools.</strong> The number of tools in the registry and the number of toolsets loaded per agent. More tools means more capability but also more tool schemas competing for space in the model's context window and more potential for the model to select the wrong tool.</li>
<li><strong>Memory.</strong> The number of entries in the semantic memory store. More memory means the agent has more context about the user's world, but also means slower searches, larger storage footprint, and more candidates to evaluate for relevance.</li>
</ul>
<p>Each axis requires different scaling techniques. The runtime applies all three simultaneously, adjusting resource allocation dynamically based on current load and device capabilities.</p>
<h2>Agent scaling with resource quotas</h2>
<p>The coordinator manages the lifecycle of all running agents within the device's resource envelope. Each agent consumes memory for its session context (typically 10–100 KB per active session), its tool registry reference (shared, negligible per-agent), and its model state (significant only when the model is loaded). When multiple agents are running, the coordinator allocates resources based on priority and enforces per-agent quotas to prevent any single agent from starving others.</p>
<p>Agents are assigned a priority level at creation. Interactive agents — those the user is actively conversing with — receive the highest priority. Their sessions are never suspended, and their context windows are never compacted unless the user explicitly requests it. Scheduled agents — daily digest, email triage, research monitors — run at medium priority. They can be suspended if the device is under memory pressure, and their sessions are compacted more aggressively. Background agents — long-running research tasks, data synchronisation — run at the lowest priority. They are suspended eagerly when the device needs resources for higher-priority work and are resumed when resources free up.</p>
<p>Each agent has a quota budget: maximum tool calls per session (default 200), maximum token consumption per day (default 1M tokens), maximum execution time per turn (default 60 seconds), and maximum wall-clock session duration (default 30 minutes). Quota limits are configurable per agent and per user. When an agent exhausts its quota, the coordinator pauses the session and notifies the user. The user can increase the quota, reset the counters, or terminate the session. Quota enforcement is strict — the runtime checks the counters before every tool call and before every model request, not after the fact.</p>
<h2>Tool registry scaling with lazy loading</h2>
<p>The tool registry holds over 500 typed tools across 56 toolsets. Loading every tool schema into every agent's context is impractical — the schemas alone would consume over 50,000 tokens, leaving no room for the actual conversation. Arble solves this through lazy loading at two levels.</p>
<p>At the toolset level, only the toolsets assigned to the current agent are loaded. A research agent activates the web search, document analysis, and data extraction toolsets but does not load the smart home, communication, or scheduling toolsets. Toolset-to-agent assignment is configured when the agent is created and can be changed at runtime. The agent does not see tools from inactive toolsets — they do not appear in the model's context, and they cannot be called.</p>
<p>At the individual tool level, schemas are injected into the model context on demand rather than all at once. When the model receives a request, the router analyses the request and predicts which tools the model is likely to need. Only the schemas for those predicted tools are included in the initial context. If the model attempts to call a tool whose schema is not in the current context, the system injects the schema and re-invokes the model. The model sees the new tool in the next turn. This dynamic injection keeps the effective context window much larger than the model's raw token limit, because irrelevant tool schemas are not consuming space.</p>
<p>Tool discovery is transparent to the model. The system maintains a registry index that maps tool names and descriptions to their schemas. When the model proposes a tool call for a tool not in context, the system looks up the schema, injects it, and re-invokes the model with the schema now available. The model's next response typically includes the tool call with correct parameters, as if the schema had been there all along.</p>
<h2>Memory compaction and archival</h2>
<p>The semantic memory store grows continuously with use. A user who has been running Arble daily for a year may accumulate tens of thousands of memory entries — facts about people, projects, preferences, and past events. Without active management, the memory store would grow without bound, degrading search performance and consuming storage.</p>
<p>Memory compaction runs periodically in the background. The compactor evaluates every memory entry and scores it on three dimensions: recency (how recently was the entry accessed or referenced), importance (the importance score assigned when the entry was created, based on factors like user emphasis and repetition across sessions), and connectivity (how many other entries reference or are referenced by this entry). Low-scoring entries are candidates for compaction.</p>
<p>Compaction does not delete entries. Instead, it merges clusters of related low-importance entries into a single summary entry. For example, three separate entries about "coffee preference: oat milk latte," "favourite coffee shop: Brew Lab," and "usual coffee order time: 10 AM" might be compacted into a single entry: "Morning coffee: oat milk latte from Brew Lab around 10 AM." The summary preserves the key facts while reducing the entry count from three to one. The original entries are deleted after compaction is verified.</p>
<p>Entries older than the configured retention period (default 180 days) are archived to a compressed store. Archived entries are not included in daily memory search results, but they can be searched explicitly through the archive browser. Archival reduces the active index size by 40–60% for heavy users while preserving the data for retrieval when needed. The archive store uses SQLite's built-in compression and is stored separately from the active memory store to avoid I/O contention.</p>
<h2>Performance budgeting across axes</h2>
<p>The three scaling axes interact. Adding more agents increases tool registry usage (each agent loads its own toolsets) and memory usage (each agent retrieves memory entries for its session). The runtime manages these interactions through a unified performance budget that tracks memory, CPU, storage, and battery impact across all three axes.</p>
<p>The performance budget is calculated at startup based on the device's capabilities and current resource availability. On a device with 8 GB of RAM, the runtime budgets 2 GB for model inference, 500 MB for session contexts, 200 MB for the tool registry, and 300 MB for the memory store. The remaining memory is reserved for the operating system and other applications. When the budget for any category is approached, the runtime begins proactive compaction: idling lower-priority agents, unloading inactive toolsets, archiving older memory entries, and reducing cache sizes.</p>
<p>The budget is not a hard limit — it is a guideline that triggers optimisation. If the user explicitly starts a high-priority task that requires exceeding the budget, the runtime compacts aggressively to make room and logs the budget overrun for user awareness. The goal is to keep the system running smoothly under normal conditions while allowing the user to push beyond limits when necessary.</p>`
},

"33":{
part:"Future",
title:"Ambient Computing",
prev:{num:"32",title:"Scaling"},
next:{num:"34",title:"AI Operating Systems"},
content:`<h2>Ambient computing defined</h2>
<p>Ambient computing is the idea that computation should recede into the background of everyday life — present when needed, invisible when not. It is the opposite of the attention-economy model that dominates current technology, where apps compete for screen time through notifications, badges, and engagement-maximising interfaces. In an ambient computing model, the technology serves the user's goals without demanding the user's constant attention.</p>
<p>AI agents are the enabler of ambient computing because they can act without requiring screen time. A traditional app requires the user to open it, navigate to the correct screen, and perform an action. An AI agent can receive a goal, plan the steps, execute them, and return results — all while the user is doing something else. The user's attention is requested only when a decision is needed that the agent cannot make autonomously. This is the core pattern: the agent does the work, the user makes the choices.</p>
<p>Arble's architecture is designed for ambient operation from the ground up. The agent loop does not require a visible UI to function. The session manager, tool registry, permission gate, and memory store all operate as background services. The UI is a window into the agent's activity, not a prerequisite for it. This distinction — between the agent system and the interface to it — is what makes ambient computing possible.</p>
<h2>Background agents: the foundation</h2>
<p>Background agents are agents that run without an active UI session. They are triggered by schedules, events, or conditions rather than by direct user messages. A background agent can check email at 8 AM every weekday and present a summary as a notification. It can monitor a project board and alert the user when a task status changes. It can run a research workflow that takes 15 minutes, collecting and synthesising information while the user works in other apps.</p>
<p>Background agents use the same agent loop, tool registry, and permission gate as interactive agents. The only difference is the trigger mechanism. An interactive agent is triggered by a user message. A background agent is triggered by a cron schedule, a webhook, a geofence event, a device state change (battery level, network availability), or another agent's output. The trigger is evaluated by the coordinator, which starts the agent session, feeds the trigger data as the initial input, and monitors execution.</p>
<p>Background execution within OS constraints requires careful resource management. On iOS, background tasks are limited to approximately 30 seconds of execution time per wake-up. Arble's runtime breaks long-running background workflows into segments that fit within the OS time limit. Each segment is executed in one background task invocation. If the workflow has more segments, the runtime schedules the next background task to continue. Segment boundaries are chosen at natural break points — after a tool call completes, before the next model invocation — so the workflow progresses smoothly across multiple wake-ups.</p>
<p>Background agents have their own permission policies, typically configured with higher autonomy than interactive agents because the user is not present to approve every step. A background agent that sends a daily digest email might have auto-approve for the email tool, while a background agent that makes purchases requires explicit approval. The permission configuration for background agents is reviewed periodically by the runtime, which flags agents whose permission levels have not been reviewed in 30 days.</p>
<h2>Notifications as a two-way input channel</h2>
<p>In an ambient computing model, notifications are not just alerts — they are a bidirectional communication channel between the agent and the user. A notification can deliver information (the daily digest is ready), request a decision (approve sending this email?), or trigger an action (tap to open the full report). The user responds to notifications without opening the app, reducing friction for routine interactions.</p>
<p>Arble implements interactive notifications on both iOS and Android. Permission requests appear as notifications with action buttons: Approve, Deny, or Review. Tapping Approve executes the tool call immediately. Tapping Deny rejects it and logs the decision. Tapping Review opens the app to the approval sheet with full context. The notification includes a summary of what the agent is requesting — the tool name, a short description of the parameters, and the agent's reasoning — so the user can make an informed decision without context switching.</p>
<p>Status notifications inform the user of background task completions. The notification includes a summary of what was accomplished (3 emails summarised, 12 search results collected, 1 document drafted) and whether any actions require follow-up. The notification is interactive: tapping it opens the session that produced the result, and the user can continue the conversation from there.</p>
<p>Notifications respect the device's focus modes and notification summary settings. During a focus mode (Work, Sleep, Do Not Disturb), only high-priority notifications are delivered — permission requests for sensitive tools and critical error alerts. Status updates and routine approvals are batched into the notification summary delivered at the user's scheduled time.</p>
<h2>Live Activities and glanceable interfaces</h2>
<p>Live Activities on iOS and similar glanceable interfaces on Android provide a persistent, at-a-glance view of the agent's status without requiring the user to open the app or even unlock the phone. The Dynamic Island and Lock Screen on iOS show the current agent activity — "Researching: 3 of 5 sources complete" — with a live progress indicator. The status updates in real-time as the agent progresses through its plan.</p>
<p>Live Activities are used for three categories of agent work: tool execution progress (web searches, file operations, data analysis), multi-step skill execution (research workflow, report generation, data migration), and pending permission requests awaiting approval. Each Live Activity displays the agent name, the current step description, a progress bar or count indicator, and the elapsed time. The activity auto-dismisses when the task completes or the user interacts with it.</p>
<p>Live Activities are updated through push notifications from the runtime, which sends a status update event every time a tool call completes or a new step begins. The update payload is minimal — a step description, a progress fraction, and a completion status — keeping the bandwidth impact negligible. The Live Activity framework handles the rendering and dismissal based on the runtime's guidance.</p>
<h2>Voice interaction patterns</h2>
<p>Voice interaction is the most natural ambient interface. The user speaks a request — "what's on my calendar today?" or "draft a reply to Priya about the budget" — and the agent responds with speech or text. No screen is needed for input or output, making voice the ideal interface for hands-free scenarios: driving, cooking, walking, exercising.</p>
<p>Arble uses the device's built-in speech recognition (SiriKit on iOS, Speech Recognizer on Android) for input. Speech is converted to text on-device — no audio data leaves the device — and the text is submitted to the agent loop as a user message. The agent processes the request normally, using the full tool registry and memory store. Output is delivered through text-to-speech (AVSpeechSynthesizer on iOS, TextToSpeech on Android) or displayed as text, depending on the context and user preference.</p>
<p>Voice interaction introduces constraints that text interaction does not. Speech recognition is imperfect, especially with accents, background noise, or specialised vocabulary. The agent must handle recognition errors gracefully — asking clarifying questions, confirming its understanding, and allowing the user to correct mistakes. Output must be concise because spoken responses take time to deliver: a 100-word response takes roughly 30 seconds to speak, which feels slow compared to reading the same text in 10 seconds. The agent is configured to prefer shorter responses in voice mode, summarising rather than elaborating.</p>
<p>The long-term direction is toward push-to-talk and always-listening modes, where the agent can be addressed by a wake word and can initiate conversations when it has important information to share. These modes are subject to the same privacy guarantees as all other Arble features: audio processing stays on-device, and the wake word detection runs locally without cloud connectivity.</p>
<h2>The ambient loop and trust progression</h2>
<p>The agent loop itself is an ambient computing pattern. The user states a goal, and the agent works through it autonomously, requesting input only at decision points it cannot resolve on its own. In the early stages of use, the agent requests approval frequently — every write operation, every send operation, every unfamiliar tool call. As the user gains experience with the agent and the agent learns the user's preferences, the interruption frequency decreases. The agent fades into the background, becoming present only when its results are ready or when it needs guidance.</p>
<p>This progression from high-interruption to low-interruption is the ambient computing adoption curve. The system does not start in ambient mode — it earns the right to operate ambience through consistent, trustworthy behaviour. The permission gate and audit log are the mechanisms that enable this trust progression. The user can always verify that the agent's autonomous decisions were correct, and the agent can point to its track record as evidence that it deserves more autonomy.</p>
<p>The goal of ambient computing in Arble is not to eliminate screens or notifications entirely. It is to make screen time intentional rather than reactive — to ensure that when the user looks at the phone, it is because they want to, not because the agent demanded their attention for something that could have been handled autonomously.</p>`
},

"34":{
part:"Future",
title:"AI Operating Systems",
prev:{num:"33",title:"Ambient Computing"},
next:{num:"35",title:"Personal Intelligence"},
content:`<h2>From procedural to declarative interaction</h2>
<p>The most significant shift that AI operating systems represent is the transition from procedural interaction to declarative interaction. In a procedural paradigm — the one that has dominated computing since the 1970s — the user specifies <em>how</em> to accomplish a goal: click this button, type this command, fill this form, run this script. The user must know the steps and execute them in the correct order. The computer does exactly what it is told, no more and no less.</p>
<p>In a declarative paradigm, the user specifies <em>what</em> they want accomplished, and the system figures out the how. "Send the Q3 report to the team" becomes a single intent rather than a sequence of steps: open email, compose new message, attach file, select recipients, type subject, send. The agent handles the steps; the user provides the goal. This is the same conceptual shift that high-level programming languages made over assembly code — abstracting away the mechanical steps so the author can focus on intent.</p>
<p>Declarative interaction is not a replacement for procedural interaction. There will always be tasks where the user wants to specify exact steps — precise formatting, complex conditional logic, operations where the process matters as much as the outcome. Declarative interaction is an additional layer on top of procedural capability, chosen by the user based on the task's requirements and their own preferences.</p>
<h2>The operating system analogy</h2>
<p>The analogy to traditional operating systems is instructive for understanding what AI operating systems are and why they matter. Early computers used command-line interfaces where the user typed commands and the computer executed them — pure procedural interaction. The graphical user interface introduced a new layer: the user could point and click instead of typing commands, but the underlying paradigm remained procedural. The user still needed to know which icon to click, which menu item to select, which sequence of actions produced the desired result.</p>
<p>Mobile interfaces abstracted further, hiding file systems and process management behind app sandboxes and touch gestures. But the procedural core remained: the user must navigate to the right app, find the right screen, and perform the right action. Each new device or app requires learning a new procedural vocabulary.</p>
<p>AI operating systems represent the next abstraction layer. Instead of translating a goal into a sequence of UI actions, the user states the goal directly. The system handles the translation, invoking tools, navigating interfaces, and composing outputs. This is not a better command line — it is a fundamentally different interaction model that makes capability accessible without requiring procedural knowledge.</p>
<p>Just as the GUI did not eliminate the command line (power users still use terminals), declarative AI interaction does not eliminate procedural interfaces. It adds a new layer for tasks where the user wants to delegate the how and focus on the what.</p>
<h2>What the shift changes</h2>
<p>The transition to AI operating systems changes several fundamental properties of how users interact with technology:</p>
<ul>
<li><strong>Apps become toolsets.</strong> Today's app model requires the user to open a specific application for each task — email for messages, calendar for scheduling, documents for writing — and to manually transfer information between them. In an AI operating system, the agent treats each application as a toolset with capabilities. The user does not "open email" — the agent uses the email tool to send a message as part of a larger workflow. App boundaries become invisible to the user.</li>
<li><strong>Permissions become granular.</strong> The current permission model asks users to grant broad access at install time — "this app wants access to your camera, contacts, and location" — and the user either accepts or does not install the app. In an AI operating system, permissions are granted per action. The agent asks for permission to "read Priya's latest email" rather than "access your entire email account." The user evaluates each request in context and can always say no.</li>
<li><strong>Memory becomes cross-app.</strong> Today, information is siloed within applications. A contact's phone number lives in the contacts app, their email address lives in the email app, their meeting history lives in the calendar app. The user manually copies information between apps. In an AI operating system, the memory store holds information from all contexts and makes it available to any tool that needs it, subject to the permission gate.</li>
<li><strong>Configuration becomes conversational.</strong> Instead of navigating nested settings screens — often with hundreds of options — the user tells the agent what they want and the agent configures the system. "Remind me to leave for appointments 15 minutes earlier" updates the calendar tool's notification settings without the user finding the right settings screen.</li>
<li><strong>Workflows become composable.</strong> Instead of learning each application's automation features — AppleScript, Shortcuts, Power Automate — the user demonstrates a workflow once, and the agent records it as a skill. The skill can be refined, shared, and applied to similar tasks without programming.</li>
</ul>
<h2>Architectural primitives for an AI operating system</h2>
<p>An AI operating system is defined by its primitives — the fundamental abstractions it provides to the agents and tools that run on top of it. Arble's primitives are the components described throughout this playbook: the agent loop, the tool registry, the permission gate, the memory store, the model router, and the coordinator. Each of these is a platform primitive that tools and agents can rely on without implementing it themselves.</p>
<p>When a new tool is added to the registry, it inherits the permission gate, the context management, the error handling, and the observability logging. The tool author writes only the tool's core logic — the parameter schema and the execution function. Everything else is provided by the runtime. This is the same relationship that exists between an operating system and an application: the OS provides process management, file I/O, memory allocation, and networking; the application provides the specific functionality.</p>
<p>The platform primitive model has compounding effects. Each new tool increases the system's capability without increasing the system's complexity for the user. Each new agent can use any registered tool, inheriting the permission and memory infrastructure. Each new memory entry enriches every agent's context. The system grows in capability while remaining manageable because the primitives handle the complexity.</p>
<h2>The transition path</h2>
<p>The transition from today's app-centric model to an AI operating system model will not happen overnight. Users have decades of muscle memory invested in procedural interfaces. Applications have decades of business models built on the app-centric paradigm. The transition will be gradual, with AI operating systems first augmenting existing interfaces and eventually becoming the primary interaction layer.</p>
<p>Arble's design reflects this transition path. The chat interface is familiar and low-barrier — users can start using it immediately without changing their workflow. As they gain confidence, they can delegate more tasks to background agents, configure routing strategies, create custom skills, and rely on the agent to proactively suggest actions. The transition is user-paced, not system-enforced.</p>
<p>The long-term vision is an interface that adapts to the user's preferred level of control. Some users will want maximum autonomy for the agent, intervening only for high-stakes decisions. Others will want to approve every action, treating the agent as an assistant that proposes but does not act. The AI operating system accommodates both models and every point between, because the architecture — the agent loop, the permission gate, the tool registry — supports the full spectrum from manual to autonomous.</p>`
},

"35":{
part:"Future",
title:"Personal Intelligence",
prev:{num:"34",title:"AI Operating Systems"},
next:{num:"glossary",title:"Glossary"},
content:`<h2>Personal intelligence vs. general intelligence</h2>
<p>Personal intelligence is the cumulative capability that an AI system develops as it learns from a specific individual. It is not general intelligence — the broad, universal capability to reason about any topic that language models aim for. Personal intelligence is narrow, contextual, and deeply personalised. It knows not just how to schedule a meeting, but how <em>this</em> user prefers meetings scheduled — which calendar to use, what duration to default to, which times of day to avoid, how much notice to give, what information to include in the invitation.</p>
<p>A general-purpose model starts fresh with every conversation. It has no knowledge of the user — their habits, their preferences, their relationships, their work patterns. It can answer any question, but it answers each one without the context that would make the answer truly useful. A system with personal intelligence builds that context over time. It remembers that the user prefers email for external communication and Slack for internal. It knows that the user's deep work hours are Wednesday afternoons and schedules meetings accordingly. It understands that when the user says "the usual format," they mean a specific template used in previous conversations.</p>
<p>The distinction between general and personal intelligence is the difference between a skilled assistant who has never met you and one who has worked with you for years. The first can do the job but needs detailed instructions for every task. The second anticipates needs, adapts to preferences, and becomes more valuable with every interaction.</p>
<h2>Three sources of learning</h2>
<p>Personal intelligence builds from three distinct sources, each feeding into the memory and configuration systems:</p>
<ul>
<li><strong>Direct instruction.</strong> The user explicitly tells the agent their preferences and rules. These are stored as structured entries in the configuration system and the permission rules. "Always use my work email for calendar notifications." "Prefer Tuesday mornings for interviews." "Never share my phone number without asking." Direct instruction is the highest-authority source — it takes precedence over patterns inferred from behaviour. The user can issue instructions at any time, in natural language, and the agent extracts the rule and stores it. Instructions can be modified or revoked later.</li>
<li><strong>Observed behaviour.</strong> The agent notices patterns in the user's decisions and adapts its behaviour accordingly. If the user consistently approves email drafts addressed to their team but carefully reviews drafts addressed to external contacts, the agent learns to auto-approve team drafts and flag external ones. If the user always rephrases the agent's suggested subject lines to be shorter, the agent learns to generate shorter subject lines. Observed behaviour is stored as preference weights in the memory store, not as explicit rules. It is lower authority than direct instruction — if the user explicitly says "always use long subject lines," that overrides the observed pattern.</li>
<li><strong>Memory accumulation.</strong> Every fact stored in the semantic memory system enriches the agent's understanding of the user's context: who they work with, what projects they care about, what tools they prefer for different tasks, what information they consider important. Memory accumulation is passive — the agent does not need to infer anything from it. The memory system retrieves relevant entries and injects them into the agent's context, where the model can use them alongside its own reasoning. Unlike observed behaviour, memory does not change the agent's behaviour directly — it provides the raw material that the model uses to make better decisions.</li>
</ul>
<p>The three sources work together. Direct instruction sets the boundaries. Observed behaviour tunes the preferences within those boundaries. Memory accumulation provides the context that makes both more effective. The combination creates a personal intelligence that is both reliable (bound by explicit rules) and adaptive (continuously tuned by experience).</p>
<h2>The virtuous cycle</h2>
<p>Personal intelligence creates a virtuous cycle that compounds over time. The more the user uses the agent, the better it understands them. The better it understands them, the more useful it becomes. The more useful it becomes, the more the user relies on it, generating more data for the learning sources to work with.</p>
<p>The cycle has three phases:</p>
<ol>
<li><strong>Accumulation.</strong> Each conversation generates new memory entries: facts about projects, preferences expressed through approvals and denials, patterns in the user's behaviour. The memory store grows richer with every session. The observed behaviour model refines its predictions with every decision.</li>
<li><strong>Synthesis.</strong> The agent applies accumulated knowledge to new situations. A request that would have required multiple clarifications on day one is handled correctly on day thirty because the agent knows the user's preferences. The agent proposes actions that align with the user's observed patterns, increasing the likelihood of auto-approval and reducing interruptions.</li>
<li><strong>Trust.</strong> As the agent consistently makes correct decisions and respects boundaries, the user grants it more autonomy. Auto-approve ranges expand. Background agents are trusted with more complex tasks. The user spends less time reviewing and approving, and more time benefiting from the agent's work. Higher trust generates more usage, which feeds back into accumulation.</li>
</ol>
<p>The virtuous cycle depends entirely on on-device learning. The user's data never leaves their device. The improvement in personal intelligence comes from local memory accumulation and behaviour analysis, not from sending data to a training server. This is not a data extraction loop — it is a local optimisation loop that makes the agent better for the user without making the model provider richer.</p>
<h2>Privacy-preserving personalisation</h2>
<p>Personalisation usually comes at the cost of privacy. Services that learn your preferences do so by collecting your data on their servers, analysing it, and updating their models. Arble's personalisation is different: all learning happens on-device, and the learned preferences stay on-device.</p>
<p>The observed behaviour model runs locally. When the agent detects a pattern in the user's approval decisions — for example, consistently approving email drafts to a specific contact — it updates a local preference weight. The weight is stored in the on-device memory store alongside other personal data. No pattern information is transmitted to any server. The preference weights are specific to the user's device and cannot be extracted or used by any other system.</p>
<p>The aggregation of personal intelligence across devices requires sync, which is encrypted end-to-end. If the user has Arble on their phone and laptop, the personal intelligence accumulated on one device is synced to the other. The sync data is encrypted before it leaves the device and can only be decrypted by the paired devices. The sync relay — if one is configured — stores only encrypted blobs and cannot read the preference weights or memory entries being synchronised.</p>
<p>The user has full visibility into what the agent has learned about them. The personal intelligence dashboard shows the explicit rules the user has set, the patterns the agent has observed (with the option to dismiss incorrect ones), and the memory entries that inform the agent's decisions. Any learned preference can be reviewed, modified, or deleted at any time.</p>
<h2>The end state</h2>
<p>Personal intelligence is the goal of the AI operating system. Not a smarter model — no amount of general intelligence can match a system that has spent months learning one person's world. The end state is an agent that knows the user well enough to act on their behalf with minimal instruction, minimal supervision, and maximal reliability. An agent that has earned enough trust to operate autonomously in the user's interest, because it has demonstrated — over hundreds or thousands of interactions — that it understands what the user wants and respects the boundaries the user has set.</p>
<p>This is what Arble is built for. Not to be another chat assistant with a slightly better model, but to be the foundation of a personal intelligence that grows with the user over years. The architecture — the agent loop, the memory store, the permission gate, the router, the coordinator — is designed for compounding improvement. Each interaction enriches the system. Each session deepens its understanding. Each approval builds trust. The agent on day one is useful. The agent on day one thousand knows the user better than any general model ever could.</p>`
},

"glossary":{
part:"Reference",
title:"Glossary",
prev:{num:"35",title:"Personal Intelligence"},
content:`<h2>Terms and definitions</h2>
<p>Complete reference of terms used throughout the Arble Playbook. Each definition includes the term's meaning in the context of Arble's AI operating system.</p>
<dl class="pbp__glossary">
<dt>Agent</dt><dd>An autonomous system that uses a language model to make decisions, call tools, and persist state across multiple turns. Runs in a loop — observe, think, act, repeat — and can operate interactively or in the background. Each agent has its own session, tool registry, and permission level.</dd>
<dt>Agent loop</dt><dd>The core execution cycle of an AI agent: receive input, process with a language model, execute tool calls, observe results, and repeat until the goal is achieved. Arble's agent loop has five explicit stages: Understand, Plan, Execute, Verify, Complete.</dd>
<dt>Ambient computing</dt><dd>A computing paradigm where technology recedes into the background and serves the user's goals without demanding constant attention. In Arble, ambient computing is enabled by background agents, interactive notifications, Live Activities, and voice interaction patterns that minimise screen time for routine tasks.</dd>
<dt>Attention</dt><dd>A mechanism in transformer models that lets each token weigh the importance of every other token in the sequence. The reason transformers can handle long-range dependencies in text. Standard attention has quadratic complexity in sequence length, which motivates optimisation techniques like flash attention and KV caching.</dd>
<dt>Autoregressive generation</dt><dd>The process by which language models produce output: one token at a time, each conditioned on all previously generated tokens. This is why generation time scales with the number of output tokens and why streaming can deliver tokens incrementally as they are produced.</dd>
<dt>Background agent</dt><dd>An agent that runs without an active UI session, triggered by schedules, events, or conditions. Background agents use the same runtime infrastructure as interactive agents but operate within OS-imposed time limits and typically have more permissive autonomy settings.</dd>
<dt>Byte-Pair Encoding (BPE)</dt><dd>A subword tokenisation algorithm that iteratively merges the most frequent adjacent byte pairs to build a fixed-size vocabulary. Used by most modern language models to convert text into tokens. BPE produces efficient encodings where common words become single tokens while rare words are split into multiple tokens.</dd>
<dt>Certificate pinning</dt><dd>A security technique that associates a host with its expected certificate or public key, preventing man-in-the-middle attacks even if a certificate authority is compromised. Arble uses certificate pinning for local network pairing between mobile and desktop agents, where the desktop agent's self-signed certificate fingerprint is verified on each connection.</dd>
<dt>Chain-of-thought</dt><dd>A prompting technique where the model is asked to show its reasoning step by step before producing a final answer. Chain-of-thought dramatically improves accuracy on multi-step problems by forcing the model to allocate intermediate tokens to reasoning steps rather than jumping directly from question to answer.</dd>
<dt>Classifier (router layer)</dt><dd>The first layer of Arble's model router. The classifier analyses incoming requests to extract features — task type, complexity, modality requirements, data sensitivity — that guide endpoint selection. Classification completes in under 5 milliseconds using pattern matchers and a lightweight local model.</dd>
<dt>Cloud model</dt><dd>A language model hosted on remote servers and accessed through an API. Cloud models typically offer larger context windows, higher accuracy on complex tasks, and multimodal capabilities that local models currently cannot match. Arble supports seven cloud provider families through normalised adapters.</dd>
<dt>Compactor</dt><dd>The runtime component that manages the model's context window by pruning low-signal turns and summarising blocks of conversation to stay within token limits. The compactor scores each turn by signal value and removes those that contribute least to the current conversation. Also referred to as the session compactor.</dd>
<dt>Context window</dt><dd>The maximum number of tokens a language model can process in a single request. All conversation history, tool schemas, system prompts, and memory results must fit within this window. Arble's session compactor actively manages context to keep signal density high and prevent the window from filling with irrelevant content.</dd>
<dt>Coordinator</dt><dd>The system service that manages the lifecycle of all running agents — creation, scheduling, suspension, termination, and inter-agent communication. The coordinator enforces per-agent resource quotas and priority-based resource allocation. It operates on structured data and rules, not a language model.</dd>
<dt>Cross-device sync</dt><dd>The synchronisation of agent state — memory, permissions, configuration, sessions — across devices without requiring a central server. Arble's sync system uses a local-first, CRDT-inspired architecture with end-to-end encryption. Each device writes locally first and reconciles with peers when connected.</dd>
<dt>Declarative interaction</dt><dd>An interaction model where the user specifies what they want to accomplish rather than how to accomplish it. The system determines the steps and executes them. This is the shift that AI operating systems enable, moving from procedural commands ("click here, type there") to intent-based requests ("send the report to the team").</dd>
<dt>Embedding</dt><dd>A dense vector representation of text produced by an embedding model. Embeddings capture semantic meaning: similar texts produce similar vectors. Arble uses embeddings for memory search, comparing the embedding of a query against the embeddings of stored memory entries to find semantically relevant results.</dd>
<dt>Endpoint (model)</dt><dd>A specific model instance accessible through a URL or IPC channel. Each endpoint has a capability profile — supported task types, cost per token, latency distribution, privacy tier, and health status. The router selects endpoints based on request requirements and current endpoint health.</dd>
<dt>Episodic memory</dt><dd>The conversation history within a single session. Episodic memory is managed by the session manager and compactor and does not persist after the session ends unless explicitly stored in semantic memory. It contains the full turn sequence of the current conversation.</dd>
<dt>Executor (router layer)</dt><dd>The third layer of Arble's model router. The executor sends the request to the selected endpoint, manages the HTTP or IPC connection, monitors the response, and handles failures by walking the fallback chain. It is the only router layer that makes network calls.</dd>
<dt>Fallback chain</dt><dd>An ordered list of model endpoints that the router tries sequentially when the primary endpoint fails. The fallback chain is generated dynamically based on request classification and the active routing strategy. It can mix local and cloud endpoints arbitrarily and is configurable per session.</dd>
<dt>Flash attention</dt><dd>An exact but memory-efficient implementation of the standard attention mechanism that avoids materialising the full attention matrix in memory. Flash attention enables longer context windows on memory-constrained devices by reducing the memory footprint of the attention computation from O(n²) to O(n).</dd>
<dt>FTS5 (Full-Text Search)</dt><dd>SQLite's built-in full-text search engine, used by Arble for searching memory entries and session history. FTS5 provides fast indexed search with support for prefix queries, phrase queries, and Boolean operators. It enables users to search across all conversations for specific terms or phrases.</dd>
<dt>Generation (inference stage)</dt><dd>The third stage of the inference pipeline, where the model produces output tokens autoregressively. Generation time depends on model size, quantisation level, hardware acceleration, and the number of tokens being generated. This stage accounts for the majority of inference latency.</dd>
<dt>Health check (endpoint)</dt><dd>A periodic lightweight request sent to each model endpoint to verify it is functioning correctly. Failed health checks increment a failure counter that eventually marks the endpoint as degraded. Health checks run every 60 seconds for cloud endpoints and every 120 seconds for local endpoints by default.</dd>
<dt>Hybrid execution</dt><dd>Routing different sub-tasks within a single request to different models — some local, some cloud — based on each sub-task's requirements. Hybrid execution enables privacy routing (local model for sensitive data), cost optimisation (cheap model for routine parts), and capability bridging (vision model for image analysis).</dd>
<dt>Inference</dt><dd>The process of running a trained language model on input to produce output. Inference in Arble is a five-stage pipeline: preparation, transmission, generation, processing, and post-processing. Distinct from training, which adjusts the model's weights over millions of examples.</dd>
<dt>KV cache</dt><dd>Keys and Values from previous tokens cached during autoregressive generation so they do not need to be recomputed for each new token. The KV cache is why generation becomes faster after the first token — only the new token's keys and values need to be computed for each subsequent step.</dd>
<dt>Lazy loading (tools)</dt><dd>A technique for managing large tool registries where tool schemas are injected into the model's context on demand rather than all at once. The router predicts which tools the model is likely to need for a given request and injects only those schemas. If the model calls an unexpected tool, its schema is injected dynamically.</dd>
<dt>Live Activity</dt><dd>A persistent, glanceable interface on iOS (Dynamic Island and Lock Screen) that shows real-time agent status without requiring the user to open the app. Live Activities display the current tool execution, skill progress, or pending permission requests with live progress indicators.</dd>
<dt>Local model</dt><dd>A language model that runs on the device's own hardware rather than in the cloud. Local models provide complete privacy (data never leaves the device) and offline capability. Arble supports local models through Ollama, llama.cpp, and Core ML backends, with automatic quantisation selection based on device capabilities.</dd>
<dt>mDNS (Multicast DNS)</dt><dd>A protocol for service discovery on local networks. Used by Arble for discovering paired desktop agents. The desktop agent broadcasts its presence and a pairing token via mDNS, which the mobile app detects to initiate the pairing handshake.</dd>
<dt>MCP (Model Context Protocol)</dt><dd>A protocol for exposing tools and resources to AI agents. Arble's desktop agent exposes its capabilities — file system, shell, screen, clipboard, browser — as MCP tools that the mobile agent can call over the local network connection.</dd>
<dt>Memory compaction</dt><dd>The process of merging related low-importance memory entries into a single summary entry to reduce the size of the semantic memory store. Compaction preserves key facts while reducing entry count. It runs periodically in the background and targets entries with low recency, importance, and connectivity scores.</dd>
<dt>Memory (procedural)</dt><dd>Skills and workflows — reusable procedures the agent can load and execute. Stored as structured JSON with steps, parameters, conditions, and error handling. Procedural memory captures "how to do things" and can be built-in, recorded from demonstrations, or written explicitly.</dd>
<dt>Memory (semantic)</dt><dd>Facts, preferences, and knowledge extracted from conversations and stored for cross-session retrieval. Persists in a vector database with FTS5 indexing. Semantic memory is what allows the agent to remember information from one conversation in a later one without being told again.</dd>
<dt>MMKV</dt><dd>A fast key-value storage library developed by WeChat, used by Arble for configuration, cached data, session metadata, and other small data items. MMKV provides millisecond read/write performance and memory-mapped file storage, making it suitable for frequently accessed data.</dd>
<dt>Model router</dt><dd>The component that selects which model endpoint to use for each request based on task type, cost, latency, privacy requirements, and endpoint availability. Arble's router has a three-layer architecture (classifier, selector, executor) and supports five routing strategies.</dd>
<dt>Multi-agent</dt><dd>A configuration where multiple agents run simultaneously, each with its own session, tool registry, and permission level. Multi-agent architectures provide isolation (agents handling sensitive data do not share context with untrusted agents) and specialisation (each agent carries only the tools relevant to its domain).</dd>
<dt>Neural Engine</dt><dd>Apple's dedicated hardware accelerator for machine learning inference, available on A17+ and M-series chips. Arble uses the Neural Engine for local model inference, running Core ML-converted models at significantly higher speed and lower power consumption than CPU-only execution.</dd>
<dt>Notification as input</dt><dd>The use of interactive notifications as a bidirectional communication channel between the agent and the user. Notifications can deliver information, request decisions (approve/deny tool calls), or trigger actions — all without opening the app. This is a core pattern in ambient computing.</dd>
<dt>Ollama</dt><dd>A popular local model runner for macOS, Linux, and Windows. Arble supports Ollama as a local inference backend on desktop platforms. The desktop agent can include an integrated Ollama client, and the model router treats Ollama endpoints identically to cloud endpoints.</dd>
<dt>Orchestration</dt><dd>The coordination of multiple components — models, tools, agents, memory — to produce a result that no single component could produce alone. In Arble, orchestration happens at the micro level (the agent loop within a single turn) and the macro level (the coordinator managing multiple agents).</dd>
<dt>Permission gate</dt><dd>The security component that evaluates every tool call against the user's configured rules and decides whether to auto-approve, ask, or block. The permission gate is implemented in trusted code that the agent cannot modify or bypass. It is the enforcement point for Arble's least-privilege security model.</dd>
<dt>Personal intelligence</dt><dd>The cumulative capability an AI system develops as it learns from a specific individual — their preferences, habits, knowledge, and working patterns. Personal intelligence builds from direct instruction, observed behaviour, and memory accumulation. It is distinct from general intelligence and becomes more valuable the longer the system operates.</dd>
<dt>Planning</dt><dd>The second stage of Arble's five-stage agent loop, where the model produces a structured sequence of steps to achieve the user's goal. Each step has a goal, a set of candidate tools, success criteria, and fallback actions. Plans are represented as structured data and can be modified at runtime when new information is encountered.</dd>
<dt>Post-processing (inference stage)</dt><dd>The fifth stage of the inference pipeline, where the session compactor evaluates whether pruning is needed, the memory manager scans for extractable facts, and usage metrics are updated. Post-processing runs after every model response and completes in under 50 milliseconds.</dd>
<dt>Preparation (inference stage)</dt><dd>The first stage of the inference pipeline, where the context is assembled, tokenised, and measured against the model's window limit. Preparation also checks the response cache — if a cached response exists for the prepared input, the remaining inference stages are skipped entirely.</dd>
<dt>Processing (inference stage)</dt><dd>The fourth stage of the inference pipeline, where the model's response is parsed, tool calls are extracted and validated against the tool registry, text content is sanitised, and the results are added to the session as a new turn.</dd>
<dt>Prompt cache</dt><dd>A caching mechanism provided by some cloud model providers that automatically detects repeated system prompt prefixes and serves the cached key-value representations. Prompt caching is most effective in multi-turn sessions where the system prompt and early conversation history are identical across consecutive requests.</dd>
<dt>Quantisation</dt><dd>Reducing the precision of model weights from floating-point (typically 16-bit) to lower precision (8-bit, 4-bit, or 2-bit). Quantisation reduces memory usage and improves inference speed with a trade-off in output quality. A well-tuned 4-bit quantisation of a 7B parameter model retains 95%+ of full-precision quality while using 75% less memory.</dd>
<dt>QueryEngine</dt><dd>Arble's core orchestrator that runs the agent loop. The QueryEngine manages conversation state, invokes the model through the router, routes tool calls to the registry, handles errors and retries, and drives the five-stage Understand-Plan-Execute-Verify-Complete loop.</dd>
<dt>Response cache</dt><dd>A local cache that stores model responses keyed on a SHA-256 hash of the full prepared input. If the same input is presented again, the cached response is returned without calling the model. Response cache entries are invalidated when any component of the input changes and are automatically pruned using LRU eviction.</dd>
<dt>Screen understanding</dt><dd>The ability of an AI system to interpret what is displayed on a screen — identify UI elements, read text, understand layout, and determine what actions are possible. Arble's screen understanding pipeline captures, analyses (via a vision model), and acts on screen content using the accessibility tree and input simulation.</dd>
<dt>SecureStore</dt><dd>Arble's abstraction over platform-specific secure storage — iOS Keychain and Android EncryptedSharedPreferences. Used to store sensitive credentials including cloud model API keys, pairing certificates, and encryption keys. SecureStore values are never written to disk unencrypted and are zeroed from memory after use.</dd>
<dt>Selector (router layer)</dt><dd>The second layer of Arble's model router. The selector matches the classified request against available endpoints by computing a score for each eligible endpoint. The scoring function weights the request's requirements against each endpoint's capability profile and supports five configurable routing strategies.</dd>
<dt>Session</dt><dd>A single conversation with an agent. Contains a sequence of turns with user messages, model responses, tool calls, and tool results. Each session belongs to one agent and has a lifecycle: creation, active, paused, completed, archived. Sessions are isolated from each other and are persisted for later review.</dd>
<dt>Session compactor</dt><dd>The component that monitors context window usage and prunes or summarises low-value turns to stay within token limits. Also referred to simply as the compactor. The session compactor runs after every turn and every tool call result, scoring each turn by signal value and removing those that contribute least.</dd>
<dt>Skill</dt><dd>A reusable multi-step workflow encoded as structured JSON. A skill combines tool calls, parameter bindings, conditional branches, success criteria, permission requirements, and error handling. Skills are distinct from tools — a tool is a single atomic operation, while a skill is a multi-step procedure that may use many tools.</dd>
<dt>Streaming</dt><dd>Delivering model output tokens incrementally as they are generated rather than waiting for the full response. Streaming reduces perceived latency from the total generation time to the time-to-first-token. Arble supports streaming from all cloud providers and local backends through a provider-agnostic adapter layer.</dd>
<dt>Subword tokenizer</dt><dd>A tokeniser that splits text into subword units — components of words that may be smaller than full words but larger than individual characters. Byte-Pair Encoding (BPE) is the most common subword tokenisation algorithm. Subword tokenisers handle rare and unknown words gracefully by breaking them into known subword pieces.</dd>
<dt>Token</dt><dd>The atomic unit of text that a language model processes. Approximately 4 characters in English. Models have fixed vocabularies of 32,000 to 128,000 tokens. Common words become single tokens while rare words are split into multiple tokens. Token count determines context usage, API cost, and inference latency.</dd>
<dt>Tokenizer</dt><dd>The component that converts text strings into sequences of token IDs that the model can process, and converts token IDs back into text. The tokeniser uses a fixed vocabulary learned during training. Different models use different tokenisers with different vocabularies, so the same text can produce different token counts in different models.</dd>
<dt>Tool</dt><dd>A single atomic operation the agent can perform — send an email, search the web, read a file, execute code. Each tool is declared with a typed schema (name, description, parameters, return type, side effects) and is registered in the tool registry. The model proposes tool calls; the system executes them through the permission gate.</dd>
<dt>Tool call</dt><dd>A structured JSON object produced by a language model that specifies a tool name and parameter values. The tool call is intercepted by the runtime, validated against the tool registry schema, evaluated by the permission gate, and executed if approved. The result is injected back into the model's context for the next reasoning step.</dd>
<dt>Tool registry</dt><dd>The catalog of all available tools in the runtime, organised into toolsets. Each tool has a name, description, parameter schema, return type, side effect declaration, and implementation function. Arble's tool registry holds over 500 typed tools across 56 toolsets, with lazy loading for efficient context management.</dd>
<dt>Toolset</dt><dd>A coherent group of related tools organised by domain — communication tools, file tools, search tools, smart home tools. Toolsets can be enabled or disabled per agent, keeping each agent's context focused on the tools relevant to its purpose. Only the active toolsets' schemas are loaded into the model's context.</dd>
<dt>Transformer</dt><dd>The neural network architecture that underlies modern language models. Uses stacked layers of self-attention and feed-forward networks to process all tokens in parallel. Introduced in the 2017 paper "Attention Is All You Need," the transformer replaced recurrent networks by enabling parallel training on much larger datasets and longer sequences.</dd>
<dt>Transmission (inference stage)</dt><dd>The second stage of the inference pipeline, where the prepared context is delivered to the model endpoint. Transmission time is dominated by network round-trip for cloud calls (50–300 ms) and by context serialisation for local calls (under 10 ms). This stage handles authentication, connection pooling, and TLS handshake management.</dd>
<dt>Turn</dt><dd>A single exchange in a session. Contains a role (user, assistant, tool, or system), content (text, tool call JSON, or tool result), and metadata including token count, timestamp, model used, and permission decisions. Turns are the atomic units of session history and are scored by the compactor for retention decisions.</dd>
</dl>`
}
