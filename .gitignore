# Python bytecode from _build/
__pycache__/
*.pyc

# macOS
.DS_Store

# Editor / tooling scratch
.vscode/
.idea/
*.swp

# Claude Code — machine-local permissions, not shared config
.claude/settings.local.json

# NOTE: docs/ is generated by _build/build.py but IS committed on purpose —
# the site is served as static files, so the output has to be in the repo.
