"14":{
part:"Devices",
title:"Desktop AI",
prev:{num:"13",title:"Orchestration"},
next:{num:"15",title:"Mobile AI"},
content:`<h2>The desktop as an AI platform</h2>
<p>A desktop computer is fundamentally different from a phone as a platform for AI. It has more memory, a more powerful GPU, a stable power supply, and unlimited thermal headroom. A MacBook Pro with an M3 Max chip can run a 70B parameter model at 15–20 tokens per second — usable for interactive chat and tool calling. A phone running the same model would hit thermal limits in under a minute and drain the battery in twenty. The desktop has no such constraints: sustained inference runs for hours without performance degradation, making it suitable for batch processing, long-running agent tasks, and continuous background operation.</p>
<p>But the desktop's real advantage is not raw compute. It is access. The desktop is where the most powerful tools live: code editors with language servers and debuggers, terminals with access to the full operating system, filesystems with years of accumulated data, databases, design tools, communication apps, and the browser. An AI on the desktop can reach all of these through native operating system APIs, without the sandbox restrictions and background execution limits that constrain mobile apps. A desktop agent can read any file the user can read, run any command the user can run, and control any application the user can control — all through the same permission model that governs the user's own actions.</p>
<p>Arble treats the desktop as a peer in a multi-device mesh, not a peripheral to the phone. The phone handles always-on, sensor-rich, portable AI — the assistant that is always with you, always listening for requests, always ready to act. The desktop handles heavyweight computation, deep tool integration, and complex multi-step workflows that benefit from a large screen, a keyboard, and sustained execution. The two complement each other, and the agent runtime seamlessly routes work to whichever device is best suited for it, based on capability, availability, and current load.</p>
<h2>The desktop agent architecture</h2>
<p>Arble's desktop companion is implemented as a Node.js MCP (Model Context Protocol) server. It runs as a background process on macOS, Windows, and Linux, and exposes the desktop's capabilities as typed tools that remote agents can call over a secure WebSocket connection. The server is intentionally stateless — all session state, conversation history, and memory live on the calling device, not on the desktop agent. This design means the desktop agent can be stopped and restarted without losing any context, and multiple devices can connect to the same desktop agent without state conflicts or synchronization issues.</p>
<p>The desktop agent is organized into toolset modules, each responsible for a domain of the operating system:</p>
<ul>
<li><strong>Filesystem toolset.</strong> Read, write, move, copy, delete, and search files. Supports glob patterns, recursive operations, metadata queries, and file watching with event-driven change detection. All operations go through the OS permission model — the agent operates within the user's file access rights and cannot escalate privileges. File reads return content as strings or base64-encoded binary data, and file writes use atomic writes to prevent data corruption.</li>
<li><strong>Shell toolset.</strong> Execute terminal commands with full stdout, stderr, and exit code capture. Commands run in the user's shell context with the user's environment variables and PATH. Supports configurable timeouts (to prevent runaway processes), working directories, environment variable overrides, and interactive command input. Each command execution is logged with its full output for audit and debugging.</li>
<li><strong>Screen capture toolset.</strong> Capture screenshots of the full desktop, individual windows, or custom regions. Returns image data as base64-encoded PNG or JPEG at native display resolution, including retina-quality captures on HiDPI displays. Supports multi-monitor configurations with display selection, and can capture individual application windows by PID or window title.</li>
<li><strong>Clipboard toolset.</strong> Read and write the system clipboard in multiple formats — plain text, rich text (HTML), images, and file URLs. Enables seamless data transfer between the mobile agent and desktop applications. The clipboard toolset can also monitor clipboard changes, allowing the agent to react to copy operations.</li>
<li><strong>Input simulation toolset.</strong> Send keystrokes and keyboard shortcuts, type text character by character, click and drag the mouse, scroll, and simulate touch events on touchscreen-equipped desktops. Uses platform-specific APIs — CGEvent on macOS with accessibility permissions, SendInput on Windows, and libuinput on Linux — ensuring native-level input simulation that applications recognize as genuine user input.</li>
<li><strong>System info toolset.</strong> Query OS version, hardware specifications, running processes, memory and disk usage, network interfaces, battery status, display configuration, and active user sessions. Provides a comprehensive view of the desktop's current state for diagnostics and decision-making.</li>
<li><strong>Browser toolset.</strong> Launch and control a Chromium-based browser instance — either headless or visible — for web automation. Maintains its own user profile directory for persistent cookies, sessions, and site data across agent sessions.</li>
<li><strong>App automation toolset.</strong> Interact with native desktop applications through platform accessibility APIs, enabling the agent to read UI state (buttons, labels, text fields, tables) and simulate user interactions in any running application.</li>
</ul>
<p>Each toolset is independently enabled or disabled during the pairing handshake and can be toggled at any time. The mobile agent only loads schemas for authorized toolsets into its context window, keeping the token budget focused on tools that are actually available.</p>
<h2>Desktop as a model host</h2>
<p>One of the desktop agent's most important roles is serving as a private inference endpoint for mobile devices. Modern desktop hardware can run quantized language models at usable speeds — a 7B parameter model at 4-bit quantisation runs at 30–50 tokens per second on an M-series Mac's GPU or Neural Engine, and a 70B model runs at 8–15 tokens per second on a discrete GPU like an NVIDIA RTX 4090. The desktop is always plugged in, so there are no battery concerns and no thermal throttling during sustained inference workloads that might run for minutes or hours.</p>
<p>Arble's model router treats the desktop-hosted model as just another endpoint in the routing table. When the mobile agent needs inference, the router checks all available endpoints — cloud APIs, local mobile models, and the desktop model — and selects the best fit based on cost, latency, privacy requirements, and task complexity. For privacy-sensitive tasks, the desktop model is often the optimal choice: data never leaves the local network, never passes through a cloud API, and never incurs per-token costs. A well-tuned 7B model running on a Mac Mini can handle 90% of everyday queries — summarization, extraction, classification, simple reasoning — with latency comparable to a cloud API.</p>
<p>The desktop model is managed through Ollama, which the desktop agent configures automatically on first launch. The user browses and downloads models from Ollama's library through the Arble mobile interface, and the desktop agent exposes the Ollama API through the same MCP interface as its other tools. The mobile device sees it as a standard OpenAI-compatible endpoint at <code>mcp://desktop.local:11434/v1</code> — the agent does not need to know whether the model is running locally or remotely.</p>
<p>This tiered inference approach conserves the mobile device's battery and thermal budget by offloading complex inference requests. For simple queries, the mobile device uses its own on-device model (a 1–3B parameter quantized model running on the Neural Engine). For complex reasoning, the router escalates to the desktop model. For the hardest tasks — or when both local and desktop models are unavailable — it falls back to a cloud model. Each tier has its own cost, latency, and privacy profile, and the router optimizes across all three dimensions automatically.</p>
<h2>The Electron desktop app</h2>
<p>For users who prefer a native desktop interface, Arble ships a full Electron application that runs the same agent runtime as the mobile app — the same session manager, memory store, tool registry, permission gate, and model router — but rendered in a desktop window with keyboard-driven interaction, multiple panels, and deep OS integration including tray icon, global shortcuts, system notifications, and Dock integration on macOS.</p>
<p>The Electron app follows a multi-process architecture. The main process manages the application window lifecycle, tray integration, global shortcuts, and the MCP server that exposes desktop tools. The renderer process displays the UI — conversation view with streaming responses, activity feed with real-time updates, settings panels with keyboard navigation — built with React Native for Web, sharing the same component tree and styling as the mobile app. A separate backend process hosts the agent runtime, communicating with the renderer over Electron IPC. This architectural separation means the UI can be restarted or refreshed without interrupting running agents, and scheduled background agents continue executing even when all windows are closed — the tray icon remains, and the user can open the window again at any time to check on progress.</p>
<p>The desktop app can run standalone with direct connections to cloud model APIs, or in paired mode where it shares state with the mobile app through the cross-device sync system. In paired mode, a conversation started on the phone appears on the desktop seamlessly, and the user can switch between devices without losing context or repeating instructions. The desktop also adds capabilities the phone lacks — multiple resizable panels for simultaneous conversations, drag-and-drop file attachments, rich keyboard shortcuts for power users, and integration with desktop-native notification systems.</p>
<h2>Background operation and device mesh</h2>
<p>Unlike mobile devices, desktops have no strict background execution limits. The desktop agent runs as a long-lived background process, always ready to accept commands or execute scheduled tasks. The process consumes minimal resources when idle — the MCP server listens on a local port, the mDNS responder broadcasts presence every 30 seconds, and the memory store is loaded on demand. Background agents can run indefinitely, processing scheduled workflows, monitoring filesystem changes, and responding to triggers without any user attention.</p>
<p>The desktop agent publishes its presence via mDNS (Bonjour) so that mobile devices on the same local network can discover it automatically. When a mobile device pairs, it receives a capability manifest detailing which toolsets are enabled, which models are available, and the desktop's current resource state — CPU load, memory usage, disk space, and battery status for laptops. This manifest lets the mobile device's router make informed scheduling decisions, such as preferring the desktop model when the phone is low on battery or routing filesystem operations to the desktop when the phone lacks local access.</p>
<p>If the desktop goes to sleep or loses network connectivity, the pairing is suspended gracefully. Pending requests are queued on the mobile device with exponential backoff and retried automatically when the connection is re-established. On wake, the desktop agent re-advertises its presence and the pairing resumes without manual intervention. This resilience is critical for real-world use — laptops close and open, networks disconnect and reconnect, and the agent system must handle these transitions transparently without losing work or requiring the user to re-establish connections manually.</p>\`},
"15":{
part:"Devices",
title:"Mobile AI",
prev:{num:"14",title:"Desktop AI"},
next:{num:"16",title:"Screen Understanding"},
content:`<h2>Mobile-first design</h2>
<p>Arble is built mobile-first. The entire agent runtime — the session manager, the memory store, the permission gate, the model router, and the coordinator — runs on the phone as the primary execution environment. The desktop agent is an optional companion that extends capabilities; the mobile agent is the core system that everything else connects to. This design decision reflects a simple reality: the phone is the device that is always with you, always connected, always powered on, and always ready to act.</p>
<p>Building an AI operating system for mobile means working within constraints that desktop and cloud systems do not face. Battery life, memory bandwidth, thermal limits, intermittent connectivity, and aggressive operating system background policies all shape what is possible. But the phone also has capabilities that no desktop can match: cameras, microphones, accelerometers, GPS, always-on cellular connectivity, Secure Enclave, biometric authentication, and tight integration with the user's daily life through notifications, widgets, and the share sheet. Arble is designed to exploit these advantages while engineering around the constraints.</p>
<p>The agent loop runs entirely on the device. The only component that may leave the device is the model inference call, and even that is optional when a local model is used. All session state, memory, tool execution, and permission decisions happen on the phone. There is no thin-client architecture where the phone is a remote control for a cloud agent — the phone is the agent, and the cloud, desktop, or any other endpoint is just a source of inference and extended capabilities.</p>
<h2>On-device constraints</h2>
<p>Running an agent system on a phone means respecting hard limits that shape every architectural decision:</p>
<ul>
<li><strong>Memory.</strong> A typical premium phone has 6–8 GB of RAM shared with the operating system, foreground apps, and background services. Arble's runtime uses MMKV for fast key-value storage (configuration, session metadata, cached tool results) and SQLite with FTS5 for the memory store — both libraries are designed for minimal memory overhead and operate in the low megabyte range. The session manager tracks per-session token counts and triggers compaction before memory pressure becomes critical.</li>
<li><strong>Processing.</strong> The agent loop itself is lightweight — it is primarily string manipulation, JSON parsing, and state management. The expensive computation is model inference, which runs on the Neural Engine (ANE), GPU, or CPU depending on availability. Arble's model loader prefers ANE-compatible models for their superior power efficiency, falling back to GPU for larger models and CPU for real-time streaming tasks.</li>
<li><strong>Battery.</strong> The runtime monitors power consumption per component. Inference is the dominant consumer, followed by network requests and memory operations. The battery-aware scheduler defers non-urgent inference to periods when the device is charging or connected to a power source. Background agents use iOS BGTaskScheduler and Android WorkManager to consolidate work into power-efficient windows.</li>
<li><strong>Thermals.</strong> Sustained inference generates heat. Modern phones throttle CPU and GPU performance when internal temperatures exceed thresholds. The runtime monitors the device's thermal state through platform APIs — ProcessInfo.thermalState on iOS, PowerManager on Android — and pauses or reduces agent activity when thermal stress is detected. Throttled inference is slower and less reliable, so the runtime prefers short, intense bursts of computation over sustained loads.</li>
<li><strong>Network.</strong> Mobile connections are variable. The runtime handles network transitions between Wi-Fi and cellular, brief disconnections during tunnel transits, and complete offline periods. All tool calls and model requests have configurable timeouts and retry policies. The memory store is fully local and never requires network access. Cloud model calls queue when offline and resume automatically when connectivity returns.</li>
</ul>
<h2>Mobile-specific capabilities</h2>
<p>The phone's hardware enables capabilities that are unique to mobile AI — features that no desktop or cloud AI could replicate:</p>
<ul>
<li><strong>Live Activities and Dynamic Island (iOS).</strong> Agent status is displayed at a glance — which tool is running, what the agent is working on, how many tasks are queued — without opening the app. The Dynamic Island shows a compact status indicator that expands on tap to reveal progress details. Lock Screen widgets provide always-visible status for scheduled agents.</li>
<li><strong>Share Sheet integration.</strong> Any text, link, image, or file from any app can be sent directly to the agent via the system share extension. The user selects "Send to Arble" from the share sheet, the content is injected into the active session, and the agent processes it immediately. This eliminates app-switching friction for common workflows like saving links, summarizing articles, or extracting contact information.</li>
<li><strong>Voice input and output.</strong> The agent accepts spoken requests through the device's speech recognition engine (Siri ASR on iOS, Google Speech on Android) and responds with synthesized speech. Voice interaction works even when the screen is off, enabling hands-free operation while driving, cooking, or exercising.</li>
<li><strong>Interactive notifications.</strong> Permission requests, task completions, and agent questions arrive as interactive notifications. The user can approve or deny tool calls, answer agent questions, or dismiss notifications directly from the notification center or lock screen without launching the app. This transforms the agent from an app the user must check into a background service that only requests attention when a decision is needed.</li>
<li><strong>Camera and sensors.</strong> The agent can use the camera to read text (OCR), scan QR codes, identify objects, or capture visual context for screen understanding. Accelerometer and gyroscope data can trigger actions based on device orientation or movement. Location services enable location-aware agent behaviors.</li>
<li><strong>Biometric authentication.</strong> The agent uses Face ID or fingerprint authentication to authorize high-sensitivity tool calls — payments, password reveals, privileged operations — without requiring the user to type a password.</li>
</ul>
<h2>Background execution and scheduling</h2>
<p>Mobile operating systems impose strict limits on background execution to preserve battery life and system performance. Arble works within these constraints rather than fighting them. The runtime uses the platform's official background task APIs — BGTaskScheduler on iOS, WorkManager on Android — to schedule agent activity in power-efficient windows.</p>
<p>Each background task request specifies a minimum interval, a required network state, and an estimated duration. The OS batches these requests with other system maintenance to minimize wake-ups and power consumption. When a task fires, the agent runtime has a limited window (typically 30 seconds on iOS, 10 minutes on Android with WorkManager) to complete its work. Arble's runtime breaks long-running agent tasks into chunks that fit within these windows and persists progress between wake-ups through the session manager's checkpoint system.</p>
<p>The background scheduler supports several trigger types: time-based schedules (every 6 hours), network-based triggers (agent runs when the device connects to a specific Wi-Fi network), location-based triggers (agent runs when the user arrives at or leaves a location), and push notification triggers (a remote event wakes the agent). Each trigger type uses the appropriate OS API to minimize power impact.</p>
<p>Background execution is fully transparent. All background activity is logged in the activity feed with timestamps, durations, and results. The user can review exactly what the agent did while the app was closed, approve or deny pending actions, and adjust background task schedules from the settings panel. No agent action occurs in the background without the user's knowledge and consent.</p>
<h2>The mobile runtime stack</h2>
<p>Arble's mobile runtime is built on Expo and React Native, providing a cross-platform foundation that shares business logic across iOS and Android while using platform-specific APIs for OS integration. The runtime is organized into three layers:</p>
<p><strong>The platform layer</strong> provides adapters for OS-specific functionality: background task scheduling through BGTaskScheduler and WorkManager, secure storage through iOS Keychain and Android EncryptedSharedPreferences, biometric authentication through LocalAuthentication and BiometricPrompt, and accessibility API access through UIAccessibility and AccessibilityService. These adapters present a unified interface to the layers above, so the agent runtime never needs to check which platform it is running on.</p>
<p><strong>The runtime layer</strong> hosts the agent services — QueryEngine, session manager, tool registry, permission gate, memory manager, and model router — as in-process services communicating through a typed event bus. Services are loaded lazily: the runtime starts with only core services active, and toolsets, memory indices, and agent instances are created on demand. This keeps the cold start time under 500 milliseconds and the baseline memory footprint under 50 MB.</p>
<p><strong>The UI layer</strong> renders the conversation interface, settings panels, activity feed, and permission approval sheets using React Native components. The UI subscribes to runtime events through a shared state store — session updates, token counts, tool call progress, permission requests — and updates the display reactively. The same UI component tree is shared between the mobile app and the Electron desktop app via React Native for Web, ensuring a consistent experience across platforms.</p>`
},
"16":{
part:"Devices",
title:"Screen Understanding",
prev:{num:"15",title:"Mobile AI"},
next:{num:"17",title:"Remote Control"},
content:`<h2>What screen understanding means</h2>
<p>Screen understanding is the ability of an AI system to interpret what is displayed on a screen — to identify user interface elements, read visible text, understand layout and hierarchy, and determine what actions are possible. It is the foundation of visual automation: if the agent can see what is on the screen, it can interact with it the way a human would, without requiring an API, an integration, or any prior knowledge of the application.</p>
<p>This is distinct from optical character recognition. OCR extracts raw text from an image — it tells you that the string "Submit" appears at coordinates (200, 400). Screen understanding extracts meaning from that text and its context: there is a button element with the label "Submit", it is currently in a disabled state, it belongs to a form with three input fields, and clicking it will trigger a form submission operation that the user should approve. The difference is the difference between reading and comprehending.</p>
<p>Screen understanding also differs from accessibility tree traversal, which is covered in the App Automation chapter. The accessibility tree provides a structured, machine-readable representation of the screen — exact labels, roles, states, and positions. Screen understanding works from pixels alone, making it applicable in situations where the accessibility tree is unavailable, incomplete, or unreliable. The two approaches complement each other: the accessibility tree provides precision when available, and screen understanding provides coverage when it is not.</p>
<h2>The three-stage pipeline</h2>
<p>Arble's screen understanding pipeline transforms a raw screenshot into actionable information through three stages: capture, analyse, and act.</p>
<p><strong>Stage one: capture.</strong> The system takes a screenshot of the target screen. On the mobile device, this uses the platform's screen capture APIs (UIGraphicsImageRenderer on iOS, MediaProjection on Android). On a paired desktop, it uses the desktop agent's screen capture toolset, which supports full-desktop, per-window, and region-based captures. The capture is compressed to PNG or JPEG format at a resolution sufficient for the vision model to read small text — typically the native display resolution, downscaled only when the model's input size limit requires it.</p>
<p><strong>Stage two: analyse.</strong> The screenshot is sent to a vision-capable language model. The model receives the image along with a system prompt that instructs it to identify user interface elements. The model outputs a structured JSON representation of the screen — a list of elements, each with a type (button, text field, label, image, checkbox, dropdown, navigation bar), text content (if any), bounding box coordinates (normalised to 0–1 range for resolution independence), interaction state (enabled, disabled, selected, focused), and semantic role (submit action, navigation link, search input, dismiss button).</p>
<p>The analysis prompt is carefully engineered to produce consistent, parseable output. It includes examples of common UI patterns — login forms, search results, settings panels, modal dialogs — and instructs the model to err on the side of reporting uncertainty when elements are ambiguous. The output schema is validated before it reaches the agent, and malformed responses trigger a retry with a more explicit prompt.</p>
<p><strong>Stage three: act.</strong> The structured element list is presented to the agent alongside the user's task. The agent determines which element to interact with and what action to perform — tap a button, type text into a field, swipe a carousel, scroll a list, long-press for context menu. The action is executed through the appropriate input system: accessibility actions when available (faster, more reliable), coordinate-based touch simulation when the accessibility tree is inaccessible, and keyboard input for text fields.</p>
<p>The pipeline runs in under two seconds on modern hardware for typical screen captures. The analysis stage dominates this time — the capture and action stages each take under 100 milliseconds. The agent can request re-analysis of the same screen after an interaction to confirm that the expected change occurred.</p>
<h2>Vision models and the router</h2>
<p>The analysis stage requires a model with vision capabilities — the ability to accept image inputs alongside text. Arble's model router maintains a list of vision-capable endpoints, including GPT-4o, Claude 3.5 Sonnet and Claude 4 Opus, Gemini 2.0 Pro, and local models with vision support like LLaVA and Qwen-VL. The router selects the vision model based on the same criteria it uses for text models: task complexity, latency requirements, cost constraints, and privacy sensitivity.</p>
<p>For routine screen understanding tasks — identifying buttons and text fields on a well-known app screen — a smaller, faster vision model suffices. For complex screens with dense information — data tables with dozens of rows, dashboards with multiple chart types, or web pages with intricate layouts — the router escalates to a flagship vision model with higher accuracy. The agent can also request multiple analyses of the same screenshot from different models and reconcile the results, using the most conservative interpretation when elements are uncertain.</p>
<p>The vision model may be the same model that is driving the agent's reasoning, or a separate model dedicated to visual analysis. Arble supports both configurations. When the agent model has vision capabilities, the screenshot is injected into the existing context and the model analyses it as part of its normal reasoning. When a separate vision model is used, the screen analysis runs in a sub-task — the agent sends the screenshot to the vision model, receives the structured output, and incorporates it into its context for further reasoning.</p>
<p>Screen understanding also supports video capture through repeated analysis. The agent can request a series of captures at intervals — for example, monitoring a progress bar or watching a loading animation — and receive updates as the screen state changes. The analysis cadence is configurable: the agent can specify "analyse every 2 seconds until the loading indicator disappears" or "capture and analyse when a system notification appears."</p>
<h2>Use cases in practice</h2>
<p>Screen understanding enables several categories of automation that would otherwise require custom API integrations for every application:</p>
<ul>
<li><strong>Universal app automation.</strong> The agent can navigate through any mobile or desktop app — filling forms, pressing buttons, reading content, extracting data — without any prior knowledge of the app's structure or API availability. This works for banking apps, social media apps, enterprise software, and legacy systems alike.</li>
<li><strong>Authentication flow handling.</strong> Many services require multi-step login flows with redirects, CAPTCHAs, and email or SMS verification codes. Screen understanding lets the agent read the authentication page, identify the input fields, wait for the verification code to arrive (possibly through the app automation toolset reading a messaging app), and complete the flow autonomously.</li>
<li><strong>Visual regression testing.</strong> The agent can capture a screenshot before and after an action, analyse both for differences, and report unexpected changes. This is useful for verifying that automated workflows produced the expected results.</li>
<li><strong>Dynamic content extraction.</strong> The agent can read content from screens that render dynamic data — search results, social media feeds, dashboards — and extract structured information even when the underlying data source has no API.</li>
<li><strong>Troubleshooting and diagnostics.</strong> The agent can read error messages, check settings panels, verify configuration values, and confirm that expected changes were applied, all by analysing the screen content.</li>
</ul>
<h2>Limitations and safeguards</h2>
<p>Screen understanding is a powerful capability, but it has fundamental limitations that the system must account for. Vision models can misidentify elements — hallucinating buttons that do not exist, misreading text in unusual fonts or low-contrast colour schemes, or incorrectly determining element states. Performance degrades on complex screens with dozens of overlapping elements, on screens with non-standard UI frameworks (game engines, custom-rendered interfaces), and on screens with poor lighting or glare in screen captures.</p>
<p>Arble's permission system applies to screen understanding actions the same way it applies to all other tool calls. Destructive actions — clicks that trigger writes, navigations that discard form data, submissions that send information — require user approval regardless of how confidently the model identified the target element. The agent can always see what is on the screen through the capture tool, but it cannot act on that information without passing through the permission gate.</p>
<p>The user also controls which screens the agent can capture. On mobile, screen capture within the Arble app is always allowed, but capturing other apps requires explicit permission through the platform's screen recording or media projection APIs — both of which display a system-level indicator when active. On desktop, screen capture requires accessibility permissions that the user grants during pairing and can revoke at any time. The combination of platform-level permissions, application-level permission gates, and full audit logging ensures that screen understanding operates within clearly defined and user-consented boundaries.</p>`
},
"17":{
part:"Devices",
title:"Remote Control",
prev:{num:"16",title:"Screen Understanding"},
next:{num:"18",title:"Browser Automation"},
content:`<h2>The remote control model</h2>
<p>Remote control in Arble means one device acting as a control surface for another. The phone can see a paired desktop's screen through screen understanding, send keyboard and mouse input, read and write files, execute terminal commands, and transfer data — all over a direct local network connection. The desktop can also initiate actions on the phone, though this is less common in practice because the phone is typically the primary interaction device.</p>
<p>This is not a remote desktop viewer in the traditional sense. The phone does not stream the desktop's display for a human to watch and interact with manually. Instead, the agent interprets the screen content and takes actions autonomously or with user approval. The user sees the agent's plan, the results of each step, and can intervene at any point, but the agent does the work. This distinction is critical: remote desktop software replaces the user's hands and eyes at a remote computer; Arble's remote control gives the agent hands and eyes on a remote computer.</p>
<p>The remote control capability is built on the same MCP protocol as the desktop agent itself. Each paired device exposes its capabilities as typed tools that the controlling device's agent can call. The phone's agent calls <code>desktop.screen.capture</code> to see the desktop screen, <code>desktop.input.click</code> to press a button, <code>desktop.shell.exec</code> to run a terminal command, and <code>desktop.filesystem.read</code> to access a file. From the agent's perspective, these are just tool calls — the same abstraction as any other tool — and the permission gate, audit log, and error handling all apply identically.</p>
<h2>The pairing protocol</h2>
<p>Pairing between Arble mobile and the desktop agent uses a multi-step handshake designed for security and ease of use:</p>
<ol>
<li><strong>Discovery.</strong> The mobile app scans the local network for desktop agents using mDNS (Bonjour, also known as Zeroconf). Each desktop agent broadcasts its presence on the local subnet with a service type of <code>_arble._tcp</code>. The broadcast includes the agent's display name (derived from the hostname), a protocol version number, a randomly generated pairing token valid for 5 minutes, and a list of available capabilities. The mobile app discovers all nearby agents without requiring the user to type an IP address or hostname.</li>
<li><strong>Verification.</strong> The user selects a discovered desktop agent from the list shown on the phone. Both devices display a six-digit verification code — the same pattern used by AirDrop, Bluetooth pairing, and WhatsApp Web. The user confirms that the codes match and taps "Pair" on the phone. This verification step prevents man-in-the-middle attacks on the local network by ensuring the user is physically present at both devices and can visually confirm the connection.</li>
<li><strong>Encryption key exchange.</strong> Once the verification code is confirmed, the two devices perform a key exchange using Curve25519 elliptic curve Diffie-Hellman. Each device generates an ephemeral key pair, signs its public key with its Secure Enclave-backed identity key, and sends the signed public key to the other device. The shared secret derived from the key exchange is used to encrypt all subsequent communication with ChaCha20-Poly1305 authenticated encryption.</li>
<li><strong>Capability negotiation.</strong> After encryption is established, the devices negotiate which capabilities will be available. The phone sends a list of requested toolsets; the desktop responds with the subset it can provide. The user can review and modify the negotiated capabilities before finalizing the pairing. The final capability set is persisted and restored on reconnection.</li>
<li><strong>Heartbeat and reconnection.</strong> The connection is maintained with periodic heartbeat messages every 15 seconds. If a heartbeat is missed for 60 seconds, the connection is considered lost. The devices enter a reconnection mode where the phone periodically checks for the desktop's mDNS advertisement and automatically re-establishes the encrypted connection when it reappears, without requiring the verification step again for previously paired devices.</li>
</ol>
<p>No cloud relay is involved in the pairing or communication. The entire handshake and all subsequent data transfer occur over the local network using WebSocket over TLS. The only thing that leaves the local network is the mDNS discovery traffic, which is confined to the local subnet by design.</p>
<h2>Relay mode for off-network access</h2>
<p>Local network pairing works perfectly when both devices are on the same network, but what about when the phone is on cellular and the desktop is at home? Arble supports a relay mode that tunnels the encrypted connection through a lightweight relay server. The relay sees only encrypted blobs — it has no access to the content of the communication, the verification codes, or the derived encryption keys.</p>
<p>The relay is built on ngrok, which creates a secure tunnel from the desktop agent to a publicly accessible endpoint. The desktop agent opens the tunnel when it starts and registers the tunnel URL with a randomly generated path segment that acts as a connection identifier. When the mobile device connects to the relay, it provides the same path segment, and the relay multiplexes the connection to the correct desktop agent. The relay cannot decrypt the communication because the end-to-end encryption uses keys that only the paired devices possess.</p>
<p>Relay mode is opt-in. The user must explicitly enable off-network access in the pairing settings, and the relay endpoint can be self-hosted for users who prefer not to use Arble's default relay infrastructure. When relay mode is active, the desktop agent maintains both a local mDNS advertisement for on-network discovery and a relay registration for off-network access — the mobile agent can connect through either path depending on its current network state.</p>
<h2>Capabilities exposed through remote control</h2>
<p>Once paired, the remote device exposes its full capability set as tools that the controlling agent can call. The capability set depends on which toolsets the controlling user authorized during pairing:</p>
<ul>
<li><strong>File operations.</strong> Read file contents (text, binary, or structured data), write or append to files, move and copy files and directories, delete files with optional trash-prevention, create directories, list directory contents with metadata (size, modification date, permissions), search file contents with regex patterns, and watch files for changes. All operations respect the file system's permission model — the agent cannot access files the user cannot access.</li>
<li><strong>Shell execution.</strong> Run terminal commands with configurable shell (bash, zsh, PowerShell, cmd), capture stdout and stderr separately, set environment variables, specify working directories, configure timeouts. Each shell execution returns the exit code, allowing the agent to detect and handle command failures programmatically.</li>
<li><strong>Screen capture and analysis.</strong> Capture the full desktop, a specific window by title or PID, or a custom screen region. The captured image can be analysed locally (if the desktop has a vision model) or sent to the mobile device for analysis. Multi-monitor setups are supported with explicit display selection.</li>
<li><strong>Input simulation.</strong> Mouse clicks (left, right, middle, with configurable modifiers), mouse movement to absolute coordinates or relative offsets, mouse drag, scroll (horizontal and vertical), keystroke sending with modifier support (Cmd+C, Ctrl+V), text input character by character, and touch event simulation for touchscreen-equipped desktops.</li>
<li><strong>Clipboard access.</strong> Read clipboard contents as text, rich text, or image data. Write clipboard contents for paste operations. Clipboard monitoring for change detection.</li>
<li><strong>System information.</strong> Query operating system version, hardware configuration (CPU cores, RAM, GPU, disk, display), running processes with resource usage, network interfaces and addresses, battery status, and active user sessions.</li>
</ul>
<h2>Security model for remote control</h2>
<p>Remote control is one of the most powerful capabilities in Arble, and it requires correspondingly strong security guarantees. Every remote control session operates under a multi-layered security model:</p>
<p><strong>Session-level approval.</strong> Every remote control session must be explicitly approved by the user on both devices. The first time a mobile device connects to a desktop agent, the desktop displays an approval dialog asking the user to confirm the pairing. Subsequent reconnections within the same session do not require re-approval on the desktop side (the pairing persists), but a new session on the mobile device requires explicit initiation — there is no persistent "always allow remote access" setting.</p>
<p><strong>Per-action permission gate.</strong> Each tool call within a remote control session goes through the standard permission gate. Reading a file on the remote desktop is treated the same way as reading a file on the local device: if the tool is set to "Auto" permission level, it runs without approval; if set to "Ask," it pauses for confirmation. The user can configure different permission levels for remote tools than for local tools — for example, allowing local file reads on auto while requiring approval for all remote file operations.</p>
<p><strong>Immutable audit log.</strong> Every remote action — every file read, shell command, screen capture, mouse click — is recorded in the audit log with the device name, timestamp, tool name, parameters (with sensitive values redacted), and whether the action was auto-approved or required explicit approval. The audit log is immutable and cannot be modified by the agent. The user can export the log for external review.</p>
<p><strong>Automatic session expiry.</strong> Remote control sessions automatically expire after a configurable period of inactivity, defaulting to 30 minutes. When a session expires, all remote capabilities are revoked until the user explicitly initiates a new session. The device pairing remains intact — only the active session's authorization expires — so re-establishing a session is a single tap rather than a full re-pairing.</p>`
},
"18":{
part:"Devices",
title:"Browser Automation",
prev:{num:"17",title:"Remote Control"},
next:{num:"19",title:"App Automation"},
content:`<h2>The browser as a tool</h2>
<p>Browser automation lets the agent interact with web pages as a user would — navigating to URLs, clicking elements, filling forms, reading content, and extracting data. Unlike API integrations, which require a specific endpoint for every service and are subject to rate limits, authentication requirements, and API versioning, browser automation works with any website regardless of whether it has a public API. This makes it the universal fallback for web-based tasks.</p>
<p>Arble implements browser automation through an integration with Puppeteer (for Chromium-based browsers) and, where available, through platform-specific web automation frameworks. The browser toolset is exposed through the MCP interface, so the agent calls browser tools the same way it calls any other tool — the agent's context includes a set of browser tool schemas, and the agent decides when to use them based on the task.</p>
<p>The browser toolset is not a standalone automation system. It is integrated into the agent loop: the agent navigates to a page, reads the content, decides what to do next, clicks a button, reads the next page, and so on. Each action informs the next, and the full sequence is recorded as tool calls in the session history, visible to the user and available for replay or debugging.</p>
<h2>Headless mode and visible mode</h2>
<p>Arble supports two modes of browser automation, selected automatically based on the task requirements:</p>
<p><strong>Headless mode</strong> runs the browser without a visible window. It is faster and more efficient — pages render more quickly, screenshots capture content without window decorations, and multiple tabs can be managed concurrently without visual clutter. Headless mode is the default for data extraction tasks: scraping product listings, monitoring prices, aggregating news headlines, checking account balances. The browser loads the page, the agent reads the DOM content, extracts the relevant data, and moves on. Headless mode consumes approximately 200 MB of memory per browser instance and can remain open for hours without user attention.</p>
<p>However, headless browsers are detectable. Many websites check for the presence of a visible viewport, the absence of certain browser extensions, or the specific JavaScript environment that headless browsers expose. When a website blocks headless access — or when the task requires authentication flows that involve redirects, CAPTCHAs, or JavaScript-heavy client-side rendering — the agent falls back to <strong>visible mode</strong>.</p>
<p>Visible mode launches a full browser window that the agent controls through screen understanding and input simulation. The browser appears as a normal application window on the desktop or as a full-screen web view on mobile. The agent navigates by sending click and type commands through the input simulation toolset and reads the page state through screen capture analysis. Visible mode is indistinguishable from a human user because the browser is a genuine browser application — the same browser the user would use manually. The trade-off is speed: visible mode operations take 2–5 seconds per action versus 200–500 milliseconds in headless mode.</p>
<p>The router selects the approach based on the task. A simple data extraction from a public webpage uses headless mode — fast, efficient, no user attention required. A login flow for a banking website uses visible mode — slower, but guaranteed to work because the website sees a real browser with a real viewport, real cookies, and real JavaScript environment.</p>
<h2>Navigation and interaction</h2>
<p>The browser toolset provides a comprehensive set of actions for web interaction, each returning the current page state so the agent can decide what to do next:</p>
<ul>
<li><strong>Navigate.</strong> Go to a URL and wait for the page to finish loading, including JavaScript execution, image loading, and network request completion. Returns the page title, final URL (after redirects), HTTP status code, and a content summary. Supports navigation timeout configuration and page load event customization (wait for DOM content loaded vs full load vs network idle).</li>
<li><strong>Click.</strong> Click on an element identified by visible text, CSS selector, XPath expression, or accessibility label. Waits for the element to be present, visible, and enabled before clicking. Returns the new page state after the click, including any navigation that resulted from the interaction.</li>
<li><strong>Type.</strong> Enter text into an input field, clearing existing content first or appending to it. Supports per-character typing (for sites that detect automated input) and bulk paste (for speed). Returns the updated field value for confirmation.</li>
<li><strong>Extract.</strong> Read text content from the current page or from specific elements. Supports full-page extraction, element-specific extraction by selector, attribute value extraction, and table data extraction as structured arrays. The extracted content is returned as text that the agent incorporates into its reasoning context.</li>
<li><strong>Screenshot.</strong> Capture the current viewport or the full page (including content below the fold). Returns the image for screen understanding analysis or for the user to review. Supports full-page screenshots by scrolling and stitching multiple viewports.</li>
<li><strong>Evaluate.</strong> Execute arbitrary JavaScript in the page context and return the result. This is the escape hatch for interactions that the higher-level tools cannot express — reading data from JavaScript variables, triggering custom event handlers, modifying the DOM programmatically. The agent must have appropriate permission to execute JavaScript, and the evaluation is subject to the same permission gate as any other tool call.</li>
<li><strong>Wait.</strong> Wait for a page condition before proceeding. Supports waiting for an element to appear in the DOM, become visible, disappear, reach a specific text state, or for a URL pattern to match. The agent can chain waits with timeouts and conditional logic.</li>
<li><strong>Manage.</strong> Open and close tabs, switch between tabs, reload the current page, go back and forward in history, clear browser data (cookies, cache, local storage), and manage the browser's user profile.</li>
</ul>
<p>Each action returns the current page state: the URL, title, visible text summary, and the status of any pending network requests. The agent does not need to maintain its own model of the browser state — it can observe the actual state after each action and adjust its behavior accordingly.</p>
<h2>Authentication and session management</h2>
<p>Many useful web tasks require authentication — the agent must log in to a service before it can perform useful actions. Arble's browser toolset handles authentication through multiple strategies, selected based on the sensitivity of the service and the user's preferences:</p>
<p><strong>Session cookie persistence.</strong> The browser maintains a dedicated user profile directory that persists between agent sessions. When the agent logs into a service, the session cookies are stored in the profile. On subsequent visits, the agent opens the browser with the same profile, and the service recognizes the existing session. This works for most services — the user logs in once through the browser, and the agent uses the persisted session for weeks or months until the cookie expires.</p>
<p><strong>OAuth token injection.</strong> For services that support OAuth, the agent can store refresh tokens securely in the device's keychain and inject access tokens into the browser context as needed. The agent never stores passwords — it uses the token grant flow to obtain credentials with scoped, revocable access. The token injection is performed through the Evaluate tool, setting <code>localStorage</code> or <code>sessionStorage</code> values that the service's authentication flow recognizes.</p>
<p><strong>Credential manager integration.</strong> On desktop, the browser toolset can integrate with the operating system's credential manager — iCloud Keychain on macOS, Windows Credential Manager on Windows — to retrieve saved passwords and autofill login forms. The agent requests a specific credential by domain, the credential manager prompts the user for biometric or password authentication, and the credential is used to fill the login form exactly as the browser would autofill it for a human user.</p>
<p><strong>Agent-assisted login.</strong> For services where no persisted session or credential is available, the agent can guide the user through a manual login. The agent navigates to the login page, identifies the input fields through screen understanding, and presents the user with a notification: "I need to log into ExampleService. Please enter your credentials in the browser window." The user logs in manually while the agent waits, and once the login is complete, the agent persists the new session cookies and proceeds with the task.</p>
<p>The agent never stores passwords in its own storage. Credentials are either managed by the platform's secure credential manager or handled through OAuth token flows. Session cookies are stored in the browser's profile, which is encrypted at rest and only accessible to the browser process.</p>
<h2>JavaScript execution and advanced techniques</h2>
<p>The Evaluate tool gives the agent access to the full power of client-side JavaScript. This is used for tasks that go beyond what click-and-type automation can achieve:</p>
<ul>
<li><strong>Data extraction from JavaScript-rendered content.</strong> Many modern web applications render their content through JavaScript frameworks like React, Vue, or Angular. The DOM may contain only a minimal root element, with all visible content generated client-side. The agent can use Evaluate to access the application's internal state — reading from React component state, Vue reactivity system, or Redux stores — to extract data that is not visible in the DOM tree.</li>
<li><strong>Interaction with custom UI components.</strong> Rich web applications often use custom UI components — date pickers, drag-and-drop lists, rich text editors — that do not respond to simple click and type commands. The agent can use Evaluate to trigger the component's internal methods, dispatch custom events, or modify the component's state directly.</li>
<li><strong>Performance and network monitoring.</strong> The agent can inject performance monitoring scripts that track page load times, network request timing, and JavaScript execution duration. This is useful for diagnosing slow-loading pages or identifying which resources are blocking page rendering.</li>
<li><strong>XSS-safe data extraction.</strong> When extracting data from untrusted sources, the agent uses Evaluate to serialize data in a controlled manner, preventing XSS attacks that could occur if untrusted content were injected into the agent's context unsanitized.</li>
</ul>
<p>JavaScript execution is a high-risk capability. The Evaluate tool is blocked by default and requires explicit user enablement. When enabled, all evaluations run in the page's sandboxed JavaScript environment — the agent cannot access Node.js APIs, the file system, or any resources outside the browser tab. The evaluation results are sanitized before they enter the agent's context, and the user can review all evaluated scripts in the audit log.</p>
<h2>Limitations</h2>
<p>Browser automation is powerful but not universal. Several categories of websites present challenges that even visible mode cannot fully solve:</p>
<p><strong>Advanced bot detection.</strong> Some websites use sophisticated bot detection systems that analyse mouse movement patterns, keystroke timing, scrolling behavior, and browser fingerprinting. Visible mode bypasses headless detection, but the agent's input simulation produces patterns that differ from human interaction. For these sites, the agent may fail to complete the task. In such cases, the agent reports the failure and suggests the user perform the task manually through the browser.</p>
<p><strong>Complex CAPTCHAs.</strong> Audio and visual CAPTCHAs are designed to be difficult for automated systems. The agent cannot solve CAPTCHAs reliably and will request human assistance when one is encountered. The agent navigates to the CAPTCHA page, displays it to the user, waits for the user to solve it, and then proceeds with the task.</p>
<p><strong>DOM volatility.</strong> Websites frequently update their DOM structure — CSS class names change, element IDs are regenerated, and layouts are restructured. A browser automation script that works today may break tomorrow when the site's frontend is redeployed. The agent mitigates this by using multiple element identification strategies — by text content, by role, by position — and falling back to screen understanding when selector-based identification fails.</p>
<p><strong>Performance overhead.</strong> Browser automation is slower than API calls. Each page load takes 1–5 seconds, each interaction takes 200–500 milliseconds, and complex workflows with multiple pages can take minutes. The agent plans for this overhead by preferring API-based approaches when available and only falling back to browser automation when no API integration exists for the required service.</p>`
},
"19":{
part:"Devices",
title:"App Automation",
prev:{num:"18",title:"Browser Automation"},
next:{num:"20",title:"Cross-Device Sync"},
content:`<h2>Automating native applications</h2>
<p>App automation is the native-application equivalent of browser automation. Instead of interacting with a web page through a browser, the agent interacts with a native mobile or desktop application through the operating system's accessibility framework. The agent can read the application's user interface — every button, text field, label, table, and menu — and simulate user interactions with them. This works for any application, regardless of whether it has a public API, a documented integration point, or any programmatic interface whatsoever.</p>
<p>This is different from having an API integration. An API integration lets the agent call a service's endpoints directly: send an email through the Gmail API, create a calendar event through the Google Calendar API, post a message through the Slack API. App automation lets the agent use the application the same way a human would — pressing buttons, reading labels, filling text fields, swiping through screens — and it works for any application, including those that have no API, those that charge for API access, and those that only exist as mobile apps with no web counterpart.</p>
<p>App automation is also different from screen understanding, though the two complement each other. Screen understanding works from pixels: it looks at a screenshot and identifies UI elements based on their visual appearance. App automation works from the accessibility tree: a structured, machine-readable representation of the screen that the operating system maintains for assistive technologies like screen readers. The accessibility tree provides exact element labels, types, states, and positions — far more reliable than pixel-based analysis, but only available when the application implements accessibility correctly.</p>
<h2>How it works: the accessibility tree</h2>
<p>Arble's app automation operates on the platform's accessibility tree, a hierarchical representation of every user interface element on the screen. Each element in the tree has properties that describe its role (button, text field, switch, slider, table cell), its label or accessibility identifier, its current value (the text in a text field, the on/off state of a toggle), its position and size on screen, and its interaction state (enabled, disabled, focused, selected).</p>
<p>The agent's workflow for app automation follows four steps:</p>
<ol>
<li><strong>Traverse.</strong> The agent reads the accessibility tree of the current screen. This returns a structured JSON representation of all visible elements, including their hierarchy — which elements are children of which other elements, which elements are grouped together, and what the navigation order is. The traversal is fast, typically completing in under 50 milliseconds on modern devices, because the operating system maintains the tree in memory and the agent is querying an existing data structure rather than performing visual analysis.</li>
<li><strong>Analyse.</strong> The model examines the accessibility tree in the context of the user's task. If the task is "find the settings for two-factor authentication," the model searches the tree for elements with labels containing "settings," "security," "authentication," or "two-factor." The model can also infer meaning from the tree structure: a table cell containing the string "Enable" that is inside a row labelled "Two-Factor Authentication" is clearly a toggle switch for the 2FA feature.</li>
<li><strong>Act.</strong> The agent sends an accessibility action to the target element. Supported actions include tap (click the element), long press (trigger a context menu), type text (enter a string into a text field), clear text (remove existing content), scroll (scroll the element or its parent), swipe (swipe in a direction, used for gestures like dismissing notifications), and focus (move keyboard focus to the element).</li>
<li><strong>Verify.</strong> After the action, the agent reads the accessibility tree again and compares it to the previous state. If the expected change occurred — a button label changed from "Enable" to "Disable," a new screen appeared, an error message was shown — the agent proceeds. If the change did not occur or an unexpected change occurred, the agent analyses the new tree to understand what happened and adjusts its next action accordingly.</li>
</ol>
<p>This four-step loop runs continuously as the agent navigates through an application. Each traversal captures the full screen state, so the agent builds a complete understanding of the application's interface over the course of a session.</p>
<h2>Platform-specific accessibility</h2>
<p>Each platform exposes accessibility through different APIs, and Arble's app automation adapts to each:</p>
<p><strong>iOS: UIAccessibility and the Accessibility Inspector.</strong> iOS maintains an accessibility tree that includes every UIKit and SwiftUI element. The agent accesses this tree through UIAccessibility APIs, which return the root accessibility element — typically the element representing the current screen — and allow traversal of its children, siblings, and descendants. The agent can also use the Accessibility Inspector's private APIs for higher-level operations like element searching and action dispatching. VoiceOver, iOS's built-in screen reader, demonstrates the completeness of this tree: if VoiceOver can reach an element, the agent can interact with it.</p>
<p><strong>Android: AccessibilityService and AccessibilityNodeInfo.</strong> Android requires an AccessibilityService declaration to access the accessibility tree. Arble's Android app includes an accessibility service that the user enables through the system Settings app. Once enabled, the service receives callbacks whenever the accessibility tree changes — a new screen appears, an element is updated, a dialog is shown. The agent uses the AccessibilityNodeInfo API to traverse the tree, query element properties, and perform accessibility actions. Android's accessibility tree is particularly rich because the platform requires accessibility support for all built-in UI components, and Google Play's policy encourages third-party developers to follow suit.</p>
<p><strong>Desktop: macOS Accessibility, Windows UI Automation, Linux AT-SPI.</strong> On paired desktops, the desktop agent's app automation toolset uses the platform's accessibility framework. macOS provides the Accessibility API (AX APIs), which exposes the full application UI hierarchy including standard controls, custom controls, and web content rendered in WebKit views. Windows provides UI Automation, a COM-based API that exposes element properties and supports pattern-based interaction. Linux provides AT-SPI (Assistive Technology Service Provider Interface), the same protocol used by the Orca screen reader.</p>
<p>Each platform adapter normalises the platform-specific accessibility tree into a common representation that the agent can analyse uniformly. A button on iOS, an Android Button widget, and a Windows Button control all map to the same element type with the same properties in the agent's analysis. This cross-platform abstraction lets the agent automate applications on any supported platform without platform-specific knowledge.</p>
<h2>Multi-app workflows</h2>
<p>The real power of app automation emerges when the agent coordinates actions across multiple applications in a single workflow. No single API integration can copy data from a messaging app, switch to a spreadsheet app, and paste it — but app automation can do exactly this, because it operates at the UI level where all applications are equally accessible.</p>
<p>Consider a workflow where the user asks the agent to "extract the address from the email Sarah sent me and add it to my contacts." The agent opens the email app, navigates to the message from Sarah, reads the email body through the accessibility tree, extracts the address text, opens the Contacts app, taps the add button, fills in the name and address fields from the extracted data, taps save, and returns to the agent conversation — all without the user touching the screen. Each app switch uses the platform's app switching mechanism, and the agent preserves context across app boundaries through its session memory.</p>
<p>Multi-app workflows benefit from the agent's planning capability. The agent produces a plan with steps for each app interaction, executes them sequentially, and reports the result. If a step fails — the email app shows an unexpected dialog, the Contacts app crashes — the agent detects the failure through accessibility tree analysis and adapts its plan accordingly.</p>
<h2>Safety and permissions</h2>
<p>App automation is the most invasive capability Arble provides. The agent can interact with any application on the device — banking apps, messaging apps, system settings, password managers — without any per-app integration or consent beyond the initial accessibility permission grant. This power requires correspondingly strong safety measures.</p>
<p><strong>Permission gate for every action.</strong> Every automated action — every tap, every text input, every swipe — goes through the permission gate. The agent cannot interact with any application without the permission system evaluating and approving the action. High-risk actions (sending a message, making a payment, changing a security setting) require explicit user approval. Low-risk actions (reading a label, scrolling a list) may be auto-approved if the user has configured the corresponding permission level.</p>
<p><strong>Per-app blocking.</strong> The user can block specific applications from automation. Blocked applications are invisible to the agent — their elements do not appear in accessibility tree traversals, and any attempt to interact with them is silently rejected. The block list is stored in the user's configuration, synced across devices, and immutable by the agent.</p>
<p><strong>Action confirmation for sensitive applications.</strong> For applications that the user designates as sensitive (banking, payments, password management), all actions — including reads — require explicit approval. The agent can still read the screen for its own analysis, but any action resulting from that analysis must wait for user confirmation. This prevents the agent from reading a banking app balance, then sending a payment without the user knowing.</p>
<p><strong>Complete audit trail.</strong> Every app automation action is logged with the application name, element type, action performed, and timestamp. The audit log is searchable by application name, so the user can review all agent activity within a specific app. The log is stored locally, immutable, and exportable.</p>`
},
"20":{
part:"Devices",
title:"Cross-Device Sync",
prev:{num:"19",title:"App Automation"},
next:{num:"21",title:"Runtime"},
content:`<h2>What sync means for an AI operating system</h2>
<p>Cross-device sync in an AI operating system is fundamentally different from file sync, document sync, or settings sync in traditional applications. Those systems synchronise static data — files, documents, configuration values — where the semantics of a conflict are well understood and the resolution strategies are well established. Sync for an AI operating system involves synchronising the agent's state: its memory, its permissions, its session context, and its configuration. These are not static files. They are live data structures that are being read and written by autonomous agents on multiple devices simultaneously.</p>
<p>The core challenge is consistency. A fact learned on the phone — "the user's preferred meeting time is Tuesday mornings" — must be available on the desktop within an acceptable timeframe, because the desktop-based agent might schedule a meeting that conflicts with an unavailable time slot. A permission granted on the desktop — "allow the research agent to access the code repository" — must propagate to the phone before the phone-based agent attempts to access the same repository and is denied.</p>
<p>Arble's sync system is designed for this specific problem. It does not use a central server for coordination — every device is a peer. It does not expose user data to third parties — all sync data is encrypted end-to-end. And it does not lose information during conflicts — the system preserves all writes and resolves conflicts through deterministic rules that produce the same result on every device.</p>
<h2>What gets synchronised</h2>
<p>Four categories of state are synchronised across devices, each with different consistency requirements and sync strategies:</p>
<ul>
<li><strong>Semantic memory.</strong> The most important sync target. Memory entries — facts, preferences, relationships, observations — are extracted from conversations on one device and must be available on all other devices. A memory entry has content (the fact), a source reference (which session and turn created it), a timestamp, an embedding vector for similarity search, and an importance score. Memory sync is append-only: new entries are added to every device's local store. Entries are never deleted by sync (though the local compaction process may remove low-importance entries independently).</li>
<li><strong>Permission rules.</strong> The user's permission configuration — which tools are auto-approved, which require approval, which are blocked — must be consistent across devices so the agent behaves identically everywhere. Permission rules are scalar values (a tool is in one of three states: auto, ask, or blocked), and they use last-writer-wins conflict resolution. If the user changes a permission on the phone and then changes it on the desktop, the most recent change (by timestamp) takes effect on all devices.</li>
<li><strong>Configuration.</strong> Model endpoint preferences (which cloud providers to use, which local models to prefer), toolset selections, theme settings, agent schedules, and notification preferences. Like permissions, configuration is scalar and uses last-writer-wins resolution. Configuration changes are typically rare and intentional, so conflicts are uncommon.</li>
<li><strong>Session state.</strong> Active session context, including conversation history, pending tool calls, and current plan. Session sync lets the user start a task on one device and continue on another without interruption. Session sync is the most complex category because sessions are large, fast-changing, and contain sensitive information. Only active and recently-active sessions are synchronised. Completed sessions are archived and not synced (they remain on the device where they were created).</li>
</ul>
<h2>Sync architecture: local-first and manifest-based</h2>
<p>Arble's sync architecture follows a local-first, manifest-based design that prioritises offline availability and user privacy:</p>
<p><strong>Local first.</strong> Every device writes all data to its own local store first — MMKV for key-value data (permissions, configuration), SQLite with FTS5 for semantic memory (entries, embeddings, indexes). Reads are always served from the local store. There is no network dependency for reading any data — the agent operates identically whether the device is online or offline. This is the most important design decision in the sync system: the local store is the source of truth, and the network is only used to reconcile differences between local stores.</p>
<p><strong>Manifest-based reconciliation.</strong> Each device maintains a sync manifest — a compact summary of its local state. The manifest contains, for each sync category, the timestamp of the most recent change and a content-addressed hash of the current state. When two devices connect — either over the local network or through a relay — they exchange manifests. Each device compares the received manifest to its own and identifies differences: entries that exist on the remote device but not locally, entries with different timestamps, entries with the same timestamp but different content hashes (indicating a conflict).</p>
<p>After identifying differences, each device requests the specific entries it needs from the other device. The transfer is incremental — only changed entries are transmitted, not the entire dataset. For a device that has been offline for a day, the initial sync might transfer a few dozen memory entries and a handful of permission changes, completing in under a second over a local network connection.</p>
<p><strong>CRDT-inspired data structures for memory.</strong> Memory entries use a Conflict-Free Replicated Data Type design. Each entry has a unique content-addressed identifier (derived from a hash of the entry content), and entries are immutable once created — they can only be added or pruned, never modified. This means that two devices can independently create memory entries about the same topic, and when they sync, both entries are preserved. Deduplication by content hash ensures that the same fact, independently extracted on two devices, is stored only once on each device after sync.</p>
<p><strong>Encrypted transport.</strong> All sync data is encrypted before transmission. Each device generates a sync key pair during setup, and the public keys are exchanged during device pairing. Sync data is encrypted with the recipient's public key using X25519 key agreement and ChaCha20-Poly1305 authenticated encryption. The relay server (if used for off-network sync) stores only encrypted blobs identified by opaque handle values — it has no access to the content, the sender, or the recipient.</p>
<p>When devices are on the same local network, sync happens directly over WebSocket with TLS, without passing through any relay. When devices are on different networks, the connection is tunneled through the relay server, which forwards encrypted blobs between devices. The relay cannot decrypt the data because it does not possess either device's private key.</p>
<h2>Conflict resolution</h2>
<p>Conflicts are rare in Arble's sync model because most data categories are either append-only (memory) or single-writer (sessions are typically active on only one device at a time). The category where conflicts are most likely is permissions and configuration, where both devices might change the same value at approximately the same time.</p>
<p>When a conflict is detected — the same permission rule has different values on two devices with timestamps within the same sync interval — the resolver applies deterministic rules that produce the same result on every device without requiring a central coordinator:</p>
<ul>
<li><strong>Last writer wins.</strong> The entry with the most recent timestamp is accepted. Timestamps are compared with millisecond precision using each device's local clock. Clock skew is mitigated by requiring NTP synchronization as a precondition for sync participation — devices with clocks that drift too far from the reference time are flagged and their entries may be deprioritised in conflict resolution.</li>
<li><strong>Lexicographic tiebreaker.</strong> If timestamps are identical (same millisecond), the device with the lexicographically greater device ID wins. This is arbitrary but deterministic — every device computing the same comparison arrives at the same result.</li>
<li><strong>Conflict preservation.</strong> The losing change is not discarded. It is preserved in a conflict log on each device, stored as a special entry type that the user can review. The conflict log entry includes the conflicting values, their timestamps, their originating devices, and a timestamp of when the resolution occurred. The user can override the automatic resolution by opening the conflict log and manually selecting the preferred value.</li>
<li><strong>Conflict notification.</strong> When a conflict is resolved, all paired devices receive a notification. The notification contains the rule that was in conflict, the two conflicting values, and which value was selected by the automatic resolver. The user can tap the notification to review the conflict and override the resolution if desired.</li>
</ul>
<p>This resolution approach is intentionally simple. More sophisticated strategies — CRDT merge rules, operational transformation, three-way merging — are unnecessary for the conflict patterns observed in practice. Permission and configuration changes are infrequent (typically a few per month), and the probability of two devices making conflicting changes simultaneously is negligible in normal use. Simple last-writer-wins with conflict logging handles the rare edge case correctly while keeping the implementation auditable and predictable.</p>
<h2>Sync triggers and scheduling</h2>
<p>Synchronisation is triggered by several events to balance timeliness with resource consumption:</p>
<p><strong>On-connection sync.</strong> When two paired devices establish a connection — either on the local network or through a relay — they perform a full manifest exchange and sync all differences immediately. This is the primary sync path, ensuring that devices are consistent within seconds of becoming mutually reachable.</p>
<p><strong>Periodic background sync.</strong> For devices that are paired but rarely on the same network (a phone on cellular and a desktop at home), background sync runs on a configurable interval, defaulting to every 6 hours. The background task scheduler wakes the device, checks for pending sync data, and transmits it in a compressed batch. The batch includes only entries that have changed since the last successful sync, minimising bandwidth usage.</p>
<p><strong>Event-driven sync.</strong> Certain high-priority changes trigger an immediate sync attempt: a permission change, a new device pairing, a security-related configuration update. The system attempts to deliver these changes within 60 seconds by establishing an ad-hoc relay connection if the target device is not on the local network.</p>
<p><strong>Manual sync.</strong> The user can trigger a sync at any time from the settings panel, which forces a full manifest exchange and reconciliation regardless of the last sync time. Manual sync is useful when the user wants to ensure consistency before starting a cross-device workflow.</p>
<p>Sync status is displayed in the device settings panel, showing the last sync time per connected device, the number of pending changes, and any sync errors. Each sync operation is logged in the activity feed with its duration, data volume, and the number of entries transferred in each direction.</p>`
}
